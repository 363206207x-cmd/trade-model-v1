# P258 Placeholder

This placeholder exists only to open the Draft PR for BACKEND-P258 Opportunity Push Review-Only Skeleton.

Codex must replace this file with the P258 index / summary and add/update only the authorized files in Issue #634.

Allowed scope: review-only Opportunity Push DTO / enum / rule skeleton, focused targeted test, verification document, and state/index updates.

Hard boundaries:

- Do not implement external push execution.
- Do not connect Telegram / email / webhook / app notification.
- Do not implement Readiness or point generation.
- Do not create entry / stop / TP / RR or trading paths.
- Do not add API / dashboard / scheduler / MarketQuoteClient wiring.
- Preserve `manualReviewRequired=true`, `notTradeInstruction=true`, and all no-execution defaults.
