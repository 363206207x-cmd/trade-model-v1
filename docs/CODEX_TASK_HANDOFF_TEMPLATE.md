# CODEX_TASK_HANDOFF_TEMPLATE — Codex 每轮任务模板

## 模板 A：只读审计

你现在位于 /Users/xuchao/Documents/trade-model-v1。

本轮严格只读审计，不允许修改任何文件。

请先读取并遵守：

1. AGENTS.md
2. docs/V1_FRAMEWORK_LOCK.md
3. docs/CODEX_PHASE_EXECUTION_RULE.md
4. docs/PHASE_POSITION_MONITOR_FREEZE_INDEX.md

本轮审计主题：

【在这里填写主题】

要求：

1. 不允许修改文件。
2. 不允许新增文件。
3. 不允许删除文件。
4. 不允许运行编译。
5. 不允许重构。
6. 不允许改首页 UI。
7. 不允许自动下单。
8. 不允许自动平仓。
9. 不允许把候选机会当成真实持仓。

请按下面格式输出：

一、当前阶段判断
二、已读取到的关键规则
三、扫描到的相关文件清单
四、当前已实现内容
五、当前部分实现内容
六、当前后端-only 内容
七、当前前端-only 内容
八、与 V1 框架冲突的内容
九、当前未实现内容
十、下一步最小实施建议
十一、预计需要修改的文件
十二、本轮明确不做什么
十三、如果进入实施，验收命令是什么

## 模板 B：最小实施

你现在位于 /Users/xuchao/Documents/trade-model-v1。

请先重新读取并遵守：

1. AGENTS.md
2. docs/V1_FRAMEWORK_LOCK.md
3. docs/CODEX_PHASE_EXECUTION_RULE.md
4. docs/PHASE_POSITION_MONITOR_FREEZE_INDEX.md
5. 与本轮相关的最新 closure 文档

本轮允许进入最小实施。

本轮目标：

【在这里填写一个最小交付物】

允许修改文件：

【在这里填写允许修改的文件清单】

禁止事项：

1. 不允许修改允许清单之外的文件，除非先说明原因并等待确认。
2. 不允许修改项目目录以外文件。
3. 不允许联网。
4. 不允许安装依赖。
5. 不允许大重构。
6. 不允许修改首页 UI，除非本轮明确要求。
7. 不允许改变已有 JSON 契约，除非本轮明确要求。
8. 不允许自动下单。
9. 不允许自动平仓。
10. 不允许把候选机会当成真实持仓。
11. 不允许为了通过编译删除核心逻辑。

实施后必须运行：

./mvnw clean compile

如本轮新增或修改测试，运行相关测试。

完成后必须新增或更新收口文档。

最终按下面格式输出：

一、当前阶段
二、本轮目标
三、修改文件清单
四、每个文件为什么改
五、实现内容
六、未实现内容
七、编译结果
八、测试结果
九、是否触碰禁止事项
十、本轮明确未做
十一、收口文档位置
十二、下一步最小动作

## 模板 C：编译失败修复

刚才执行 ./mvnw clean compile 失败。

请只修复本轮改动引入的编译错误。

要求：

1. 不允许扩大修改范围。
2. 不允许重构。
3. 不允许改首页 UI。
4. 不允许改无关模块。
5. 不允许为了编译通过删除核心逻辑。
6. 不允许联网安装依赖。
7. 修复后必须重新运行 ./mvnw clean compile。
8. 最后说明修改文件和原因。

编译错误如下：

【粘贴错误】

完成后输出：

一、失败原因
二、修改文件
三、修复方式
四、编译结果
五、是否扩大范围
六、下一步建议
