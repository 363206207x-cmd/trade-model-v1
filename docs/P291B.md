# BACKEND-P291B Source of Truth Operational Fill Pack

P291B is an A docs-only operational fill pack.

P291A created the source-of-truth documents. P291B fills them so a new chat window, Codex task, or PR review can determine project state from repository files without relying on chat memory.

## Baseline

Current merged `main` for this pack:

- `26e8943 BACKEND-P291A Workflow Reset and Progress Source of Truth Pack (#703)`

Known open work at pack creation:

- PR #705 `BACKEND-P292 MarketReadRequest Test-Only Wiring and Review-Only Assembler Slice`

PR #705 is not completed until merged into `main`.

## Purpose

P291B operationalizes:

- merged-main-only progress rules;
- capability level matrix;
- MVP business-chain roadmap;
- review-only allowed output rules;
- drift guard checklist;
- current state summary;
- fixed progress percentage口径;
- workflow / template / PR review enforcement.

## Scope Boundary

P291B does not change Java, tests, DTOs, dashboard, schema, config, service wiring, controller/API, mapper, repository, scheduler, market clients, runtime data reads, scan output, score, Candidate, Push, Readiness, point generation, entry / stop / TP / RR implementation, order API, execution API, or auto-trading.
