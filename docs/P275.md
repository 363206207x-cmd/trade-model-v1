# P275 External Channel Closure and CI Exception Record Scope Pack

P275 is a docs-only closure and scope pack after P274.

P275 closes the P274 external channel disabled no-op Java skeleton, records the P274 local-validation exception merge, stops further external-channel no-op expansion for now, and redirects the next planning track toward real scan / real score / Candidate / Push preview.

P275 does not write Java, tests, DTOs, dashboard code, schema, config, API, scheduler, external channel behavior, provider credentials, live provider calls, message rendering, message sending, Readiness, point generation, order/execution, or auto-trading.

## Baseline

Current main before P275:

```text
905fb41 BACKEND-P274 External Channel Skeleton (#667)
```

P274 was merged as `905fb41`.

## P274 Closure Input

P274 added:

- `OpportunityPushExternalChannelDTO`
- `OpportunityPushExternalChannelStatusEnum`
- `OpportunityPushExternalChannelPolicy`
- `NoOpOpportunityPushExternalChannelPolicy`
- `NoOpOpportunityPushExternalChannelPolicyTest`

P274 remains only a disabled-by-default / no-op external channel skeleton. It is not real external channel behavior, not Telegram/email/webhook/app notification/local notification integration, not provider credential handling, not live provider calls, not provider selection, not message rendering, not final send text, not message sending, not scheduler/API/dashboard wiring, not Readiness, not point generation, not order/execution, and not auto-trading.

## P274 CI Exception Record

P274 was a local-validation exception merge:

- GitHub CI did not trigger for PR #667 / HEAD `929841d`.
- An empty retrigger commit was attempted.
- Ready for review transition was attempted.
- Local validation passed:
  - `./mvnw -q -Dtest=NoOpOpportunityPushExternalChannelPolicyTest test`
  - `./mvnw -q -DskipTests compile`
  - `./mvnw -q -DskipTests test-compile`
- The user explicitly approved the local-validation exception merge.
- P274 was merged as `905fb41`.

This exception does not weaken future gates. Future Java changes still need the normal targeted tests and CI expectation unless a user-approved exception is explicitly recorded.

## Stop Gate

P275 stops the current external-channel no-op expansion chain.

Do not immediately continue adding more external-channel no-op layers after P275. The current push channel stack is enough for disabled review-only modeling:

- Opportunity Push review-only skeleton
- disabled no-op delivery policy
- audit-only envelope
- no-op audit persistence
- no-op audit queue
- disabled no-op delivery pipeline
- disabled no-op message envelope
- disabled no-op provider channel
- disabled no-op external channel

Further Telegram/email/webhook/app notification/local notification work remains blocked unless a later issue explicitly authorizes it.

## Return Gate

The recommended next planning direction returns to:

- real scan planning
- real score planning
- Candidate planning
- Push preview planning

This return gate is planning-only at P275. It does not authorize runtime/live/external reads, MarketQuoteClient wiring, BinanceMarketQuoteClient wiring, scan loops, production score computation, Candidate production workflow, Push execution, scheduler/API/dashboard changes, Readiness, point generation, entry/stop/TP/RR, order/execution, or auto-trading.

## Still Blocked

The following remain blocked:

- Telegram/email/webhook/app notification/local notification
- provider credentials
- live provider calls
- provider selection / provider integration
- message rendering
- message sending
- scheduler/API/dashboard wiring
- runtime/live/external data reads
- MarketQuoteClient / BinanceMarketQuoteClient scan integration
- real scan loop
- external Opportunity Push execution
- Promote To Home runtime logic
- Readiness
- point generation
- real entry/stop/TP/RR
- order API
- execution API
- auto-trading

Display Slots / 默认六币 cannot be used as the batch universe or default push universe. Watchlist Pool remains the push candidate boundary. Risk Action Guard must stay before any delivery path. Stampede state blocks opportunity push. A wick-only/pin-bar move is not a trend reversal. Strong reversal is not direct reverse trading.

## P275 Documents

P275 adds:

- `PHASE_P275_EXTERNAL_CHANNEL_NOOP_CLOSURE.md`
- `PHASE_P275_CI_EXCEPTION_RECORD.md`
- `PHASE_P275_PUSH_CHANNEL_EXPANSION_STOP_GATE.md`
- `PHASE_P275_REAL_SCAN_SCORE_CANDIDATE_RETURN_GATE.md`
- `PHASE_P275_READINESS_POINT_GENERATION_STILL_BLOCKED.md`

P275 also updates:

- `V1_CURRENT_STATE.md`
- `PROJECT_PROGRESS_INDEX.md`

## Verification

P275 is docs-only, so Maven may be skipped unless Java/test/resources are modified.

Required checks:

```bash
git diff --name-status main...HEAD
git diff --check
git status
```
