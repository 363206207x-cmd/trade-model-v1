# Trade Model V1 Delivery Progress Matrix

Contract: docs/PROJECT_DELIVERY_CONTRACT.md
Contract Version: v1.0 + P0-0 reconciliation draft

Allowed Phase Status values:

- NOT_STARTED
- IN_PROGRESS
- BLOCKED
- DONE
- DEFERRED
- FROZEN

Allowed Existing Module Maturity values:

- NONE
- PARTIAL
- COMPLETE

Rules:

1. Phase Status tracks contract-phase progress.
2. Existing Module Maturity tracks pre-existing repository assets discovered by scan.
3. Phase DONE is effective only after merged to `main`.
4. docs-only, DTO-only, review-only, preview-only, dashboard-only work cannot mark a business phase DONE.
5. P0-0 is a governance phase; it can complete by P0-0 criteria, but this does not mark business modules DONE.
6. P0-1 must remain NOT_STARTED until P0-0 is DONE on merged main.
7. A-risk docs / contract / workflow packages may auto-merge only under the A-risk Auto Merge Rule; unrelated Draft PRs do not block current package merge but still block the next business phase.

| Phase | Module | Phase Status | Existing Module Maturity | Existing Evidence | Missing Evidence | Test Evidence | Next Allowed |
|---|---|---|---|---|---|---|---|
| P0-0 | Contract Lock + Baseline + Dead Code Candidate Report | DONE | PARTIAL | PROJECT_DELIVERY_CONTRACT.md exists; PROJECT_CURRENT_STATE.md exists; DELIVERY_PROGRESS_MATRIX.md exists; CODEX_TASK_TEMPLATE.md exists; CONTRACT_CHANGE_LOG.md exists; DEAD_CODE_CANDIDATES.md exists; PROJECT_GLOBAL_AUDIT.md exists; AGENTS.md references the contract; workflow automation migrated; A-risk Auto Merge Rule added for docs / contract / workflow packages; no business code changed; no code deleted; merged main effectivity confirmed by runtime gate. | None for P0-0 merged-main effectivity. | Maven PASS; shell syntax PASS; workflow contract PASS; task validation PASS; v1-state/v1-auto contract-first checks PASS; git diff --check PASS; A-risk scope check PASS. | P0-1 UserPosition |
| P0-1 | UserPosition | DONE | COMPLETE | `tm_user_position`, `UserPositionDO`, `UserPositionDTO`, `UserPositionResponseDTO`, `UserPositionVO`, `CreateUserPositionReq`, `CloseUserPositionReq`, status/side/direction/source/source-type enums, `UserPositionMapper`, `UserPositionService`, `UserPositionController`, manual open, manual close, open query excluding CLOSED, source_type fixed to MANUAL, safety fields fixed true, and fail-closed rejection for ExecutionPlan / triggered / real_position auto-create sources are merged on main. | None for P0-1 merged-main effectivity; P0-2 is allowed only through runtime gate on clean / synced main. | UserPosition service tests PASS; controller endpoint tests PASS; mapper integration tests PASS; Maven full-suite PASS on merged main. | P0-2 ExecutionPlan Source Gate |
| P0-2 | ExecutionPlan Source Gate | DONE | COMPLETE | `ExecutionPlanSourceGate`, `BoundaryCandidateSourceGate`, `NumericBoundarySourceValidator`, source gate result fields on `ExecutionPlanVO`, source gate persistence fields on `tm_execution_plan`, fail-closed source validation, DTO.valid gate enforcement, and source gate controller/service response exposure are merged on main. Runtime gate confirmed P0-2 effective on clean / synced main after PR #1008 and workflow gate fix PR #1009. | None for P0-2 merged-main effectivity. | ExecutionPlan Source Gate tests PASS; BoundaryCandidate DTO/service gate tests PASS; PlanService source gate tests PASS; controller response test PASS; Maven full-suite PASS on merged main. | P0-3 AccountRisk integrates UserPosition |
| P0-3 | AccountRisk integrates UserPosition | DONE | COMPLETE | `UserPositionRiskAdapter`, read-only UserPosition account risk calculation, read-only AccountRisk API, OPEN/PARTIALLY_CLOSED inclusion, CLOSED exclusion, leverage / position size / concentration / conservative directional correlation / drawdown-or-VaR proxy risk, high-risk blocking, PushRecheck read-only consumption, and stable service result for future PositionMonitor consumption are merged on main by PR #1010. Runtime gate confirmed P0-3 effective on clean / synced main. | None for P0-3 merged-main effectivity. | UserPosition risk adapter tests PASS; AccountRisk API test PASS; PushRecheck integration tests PASS; Maven full-suite PASS on merged main. | P0-4 PositionMonitorLog |
| P0-4 | PositionMonitorLog | DONE | COMPLETE | `tm_position_monitor_log`, `PositionMonitorLogDO`, `PositionMonitorLogDTO`, `PositionMonitorLogMapper`, `PositionMonitorLogService`, `recordMonitorRun` one-log persistence, immutable read queries, and Review read-only monitor-log query path are merged on main by PR #1011. Runtime gate confirmed P0-4 effective on clean / synced main. | None for P0-4 merged-main effectivity. | PositionMonitorLog service tests PASS; mapper integration tests PASS; Review query tests PASS; Maven full-suite PASS on merged main. | P0-5 PositionMonitorService |
| P0-5 | PositionMonitorService | DONE | COMPLETE | `PositionMonitorService`, `PositionMonitorController`, single UserPosition monitor run, batch active UserPosition monitor run, LONG / SHORT logic, LOGIC_VALID / LOGIC_WEAKENED / PLAN_INVALIDATED / HIGH_RISK states, near stop loss, near take profit, risk increased detection, MarketQuote read, ExecutionPlan context read, AccountRisk read, and one PositionMonitorLog write per successful monitor run are merged on main by PR #1012. Runtime gate confirmed P0-5 effective on clean / synced main. | None for P0-5 merged-main effectivity. | PositionMonitor service tests PASS; PositionMonitor controller tests PASS; Maven full-suite PASS on merged main. | P0-6 Review integrates UserPosition |
| P0-6 | Review integrates UserPosition | DONE | COMPLETE | `UserPositionReviewAdapter`, CLOSED UserPosition ReviewSummary, linked ExecutionPlan read by exact planId / analysisId, real user entry / close / stop-loss / take-profit / quantity / leverage reads, all PositionMonitorLog timeline reads, win / loss / breakeven calculation, execution deviation, plan invalidation, warning timeliness, ignored warning, and manual rule feedback through existing ReviewService / rule-version audit owners are merged on main by PR #1013. Runtime gate confirmed P0-6 effective on clean / synced main. | None for P0-6 merged-main effectivity. | UserPosition review adapter tests PASS; Review controller tests PASS; PositionMonitorLog review timeline tests PASS; Maven full-suite PASS on merged main. | P1-1 PushRecheck semantic hardening |
| P1-1 | PushRecheck semantic hardening | DONE | COMPLETE | Review-only canonical Recheck statuses, RecheckResult safety fields, EXPIRED / DRIFTED_FROM_ENTRY_ZONE / RISK_BLOCKED / CONFUSED_BLOCKED semantics, legacy compatibility reads, scheduler/backlog/replay/log mappings, and no-trade guards are merged on main by PR #1014. Runtime gate confirmed P1-1 effective on clean / synced main. | None for P1-1 merged-main effectivity. | PushRecheck semantic hardening status tests PASS; controller safety tests PASS; legacy compatibility tests PASS; Maven full-suite PASS on merged main. | P1-2 ConfusedState + AiConflict hardening |
| P1-2 | ConfusedState + AiConflict hardening | DONE | COMPLETE | `ConfusedStatePolicy`, confused_score >= 70 CONFUSED entry, confused_score >= 85 directional push block, two consecutive cycles below 55 before COOLING exit, per-symbol persisted low streak, no direct TRIGGERED / WAITING_TRIGGER after exit, rule-layer direction preservation, single-AI-objection boundary, GPT / Gemini / Grok advisory-only roles, allowed AI effects limited to confidence / risk / plan mode / confused state, and PushSnapshot directional guard are merged on main by PR #1015. Runtime gate confirmed P1-2 effective on clean / synced main. | None for P1-2 merged-main effectivity. | ConfusedState threshold and transition tests PASS; AiConflict aligned / minor / major / extreme tests PASS; DecisionEngine integration tests PASS; PushSnapshot block tests PASS; Maven full-suite PASS on merged main. | P1-3 HotReset real action |
| P1-3 | HotReset real action | DONE | COMPLETE | Structured EXTREME_PRICE_MOVE / OI_COLLAPSE / LIQUIDITY_DRAIN / SYSTEMIC_SHOCK triggers, event-key idempotency, immediate CANDIDATE / WAITING_TRIGGER / TRIGGERED invalidation, safe AssetState transitions to HIGH_RISK / INVALIDATED / CONFUSED / COOLING, ExecutionPlan needs_revalidation marking, Decision HotReset invalidation metadata, pending PushSnapshot RECHECK_INVALIDATED marking, ConfusedState recalculation, AccountRisk recalculation, analysis rebuild after transaction commit, rebuild failure recording, and complete HotReset event persistence are merged on main by PR #1016. Runtime gate confirmed P1-3 effective on clean / synced main. | None for P1-3 merged-main effectivity. | HotResetPolicy tests PASS; HotResetService tests PASS; AssetState / AnalysisAssembler / PushSnapshot targeted tests PASS; Maven full-suite PASS on merged main. | P1-4 OpportunityLog |
| P1-4 | OpportunityLog | DONE | COMPLETE | `tm_opportunity_log`, `OpportunityLogDO`, `OpportunityLogMapper`, `OpportunityLogService`, `OpportunityLogController`, authoritative candidate creation after analysis persistence, exact manual UserPosition source-ref linking, legacy MissedOpportunity compatibility freeze, EXECUTED_VALID / EXECUTED_INVALID / MISSED_VALID / MISSED_INVALID / PUSHED_NOT_FILLED_VALID / BLOCKED_BY_RISK_VALID classifications, target / invalidation ordering, same-bar ambiguity, MFE / MAE from persisted OHLCV, Review opportunity stats, safety fields, and tests are merged on main by PR #1017. Runtime gate confirmed P1-4 effective on clean / synced main before P2-1 started. | None for P1-4 merged-main effectivity. | OpportunityLog service tests PASS; OpportunityLog controller tests PASS; Review stats tests PASS; MissedOpportunity compatibility tests PASS; AnalysisAssembler integration tests PASS; Maven full-suite PASS on merged main. | P2-1 Macro / News / External Context |
| P2-1 | Macro / News / External Context | DONE | COMPLETE | `tm_macro_event`, `tm_news_event`, MacroEvent / NewsEvent DO + Mapper + Service, ExternalContextEvidenceBuilder, source trace fields on EvidenceItem, event-window snapshot propagation to Decision / ExecutionPlan / PositionMonitor, ExternalContextController import/query/status endpoints, dashboard read-only panel, source health fail-closed behavior, high-impact/manual-review policy, and tests are merged on main by PR #1018 commit `d7fef874b39aabbd07f6b05fd97f4725e89e79b5`. Runtime gate confirmed P2-1 effective on clean / synced main before P2-2 started. | None for P2-1 merged-main effectivity. | P2-1 targeted tests PASS; Maven full-suite PASS on merged main. | P2-2 AI Orchestrator + AiCallLog |
| P2-2 | AI Orchestrator + AiCallLog | DONE | COMPLETE | `AiDecisionOrchestratorService`, provider abstraction and safe OpenAI/GPT, Gemini, xAI/Grok adapters, strict review-only prompt/response contract, provider timeout/failure/malformed fallback, prompt-injection guard, rate-limit and budget fail-closed guard, `tm_ai_call_log` schema/mapper/service, token/cost/latency/provider/fallback/traceId logging, read-only `/api/ai/orchestrator/status` and `/api/ai/call-logs` APIs, and DecisionEngine integration that preserves rule-layer direction while AI can only support/challenge/abstain through conflict downgrade are merged on main by PR #1019 commit `92fd7cbf17db31c8ea2bfd4673badde1c69d20cd`. Runtime gate confirmed P2-2 effective on clean / synced main before P2-3 started. | None for P2-2 merged-main effectivity. | P2-2 targeted tests PASS; Maven full-suite PASS on merged main. | P2-3 Scheduler / Idempotency / Trace |
| P2-3 | Scheduler / Idempotency / Trace | DONE | COMPLETE | Canonical idempotency key over normalized symbol / timeframe / timeframe-aware bucket / rule version, supported timeframe bucketing, fail-closed manual and scheduler inputs, DB unique-key concurrency guard, explicit claim/failure transactions, lease owner / claimVersion / attemptCount fencing, failed-run and expired-lease recovery, partial-state recovery blocking, max attempts, redacted error persistence, direct assembler bypass block, requestId / traceId / input snapshot persistence, by-request trace API, scheduler status API, and trace snapshots with COMPLETE / PARTIAL_TRACE / RUNNING / FAILED semantics are merged on main by PR #1020 commit `5c2b2b47eb7fa4cfc9c428ef022375f4ca890b23`. Runtime state confirmed clean / synced main before P3-1 started. | None for P2-3 merged-main effectivity. P3-1 and P3-2 have completed separately; P3-3 remains a separate final delivery phase. | P2-3 targeted tests PASS; Maven full-suite PASS on merged main before P3-1; runtime sync PASS on clean / synced main. | P3-1 Dashboard Final |
| P3-1 | Dashboard Final | DONE | COMPLETE | Final homepage UI layout is merged on main by PR #1023 commit `f543832cf5907fe00920ca3f05666566daa16b7a`: header status pills, seven-card system state row, risk alert / key event row, asset monitor cards, user real position monitor, non-trade execution suggestion panel, bottom AI three-role decision row, and consistency panel are present in `dashboard.html`. Legacy test anchors `candidateReviewDisplay` / `candidateReviewSkeleton()` and `internalPushPreviewDisplay` are preserved. The merged PR changed only `src/main/resources/templates/dashboard.html`. | None for P3-1 Dashboard Final UI effectivity. P3-2 Full E2E Acceptance completed separately after P3-1; production deployment readiness remains separately BLOCKED; no order / execution / auto-trading capability is introduced. | `./mvnw test -q` PASS on synced main; dashboard anchor/layout grep PASS; `bash scripts/v1-state.sh` confirms clean / synced main with `HEAD_MATCHES_ORIGIN_MAIN: YES`; open PR count is now confirmed as 0 by `gh CLI`. | P3-2 Full E2E Acceptance |
| P3-2 | Full E2E Acceptance | DONE | COMPLETE | P3-2 acceptance evidence is merged/effective on current main through PR #1025 Dashboard Manual UserPosition Binding, PR #1027 Full UserPosition lifecycle E2E acceptance, and PR #1028 Dashboard E2E state proof / commit `1b08abd`: ExecutionPlan-only / triggered DecisionResult rows do not render opened user positions; manual OPEN UserPosition is the dashboard real-position source; CLOSED and non-MANUAL UserPosition rows are excluded from open dashboard display; the UserPosition -> PositionMonitor -> manual close -> Review -> rule feedback chain is evidenced; dashboard key E2E states and safety anchors are evidenced; PositionMonitor HOLD / LOGIC_WEAKENED / PLAN_INVALIDATED are evidenced; AccountRisk high-risk blocking is evidenced; PushRecheck does not create UserPosition; ConfusedState and HotReset safety states are evidenced; Review execution deviation and rule feedback are evidenced; no order / execution / auto-trading capability is introduced. | None for P3-2 E2E acceptance closure. Production Deployment Readiness remains separately BLOCKED in P3-2A and blocks production-ready / deployment claims. | `DecisionServiceImplTest`, `DashboardControllerTest`, `StaticNoTradeInstructionGuardTest`, `CandidatePushReviewOnlyMvpClosureTest`, `UserPositionFullLifecycleE2EAcceptanceTest`, `PositionMonitorServiceImplTest`, `PositionMonitorLogServiceImplTest`, `UserPositionRiskAdapterTest`, `PushRecheckServiceImplTest`, `ConfusedStateServiceImplTest`, `HotResetServiceImplTest`, `UserPositionReviewAdapterTest`, `ReviewControllerUserPositionReviewTest`, `OpportunityLogServiceImplTest`, `MacroEventServiceImplTest`, `NewsEventServiceImplTest`, `AiDecisionOrchestratorServiceImplTest`, and `AiCallLogServiceImplTest` evidence the acceptance chain; `./mvnw test -q` PASS; `bash scripts/v1-delivery-check.sh` PASS; `bash scripts/v1-state.sh` PASS with blockers none on clean/synced main. | P3-3 Final Delivery & System Freeze |
| P3-3 | Final Delivery Docs / System Freeze | DONE | COMPLETE | P3-3 final freeze is recorded as local/acceptance-ready on current main. Dashboard Home Aggregation API exists and `/api/dashboard/home` returns `200 success` with `header`, `systemState`, `assets`, `positions`, `executionSuggestion`, `aiDecision`, `pushInbox`, `diagnostics`, and `safety`. Dashboard Data Fill P1-P5 are merged/effective: decision/systemState/assets, manual positions/executionSuggestion, AI role evidence, pushInbox readonly data, and Telegram readonly status contract. Review Center readonly integration exists: `/review/dashboard` and `/api/review/center`; `/api/review/center` returns `summary`, `positionReviews`, `opportunityReviews`, `pushReviews`, and `ruleFeedback`; four tabs are readonly with no fake records. Mainline validation is clean main, no open PRs, MAIN_SYNC OK, full tests PASS, delivery check PASS, and v1-state blockers none. | Production Deployment Readiness remains separately BLOCKED in P3-2A. No production deployment approval is granted; no Telegram send, Push send, order, execution, or auto-trading capability is introduced. | `./mvnw test -q` PASS; `bash scripts/v1-delivery-check.sh` PASS; `bash scripts/v1-state.sh` PASS with blockers none; `curl -s http://localhost:8081/api/dashboard/home` returned `200 success`; `curl -s http://localhost:8081/api/review/center` returned `200 success`. | Local acceptance handoff / production readiness remains blocked |
| P3-U1 | Personal Login + Session Authentication | IN_PROGRESS | PARTIAL | Branch `codex/p3-u1-personal-login-session-auth` locally validates Thymeleaf form login, server-side Session, BCrypt-backed `tm_user`, success-only `last_login_at`, bounded failure limiting, sanitized login/logout audit, CSRF-protected browser writes, fixation migration, HttpOnly/SameSite Cookie policy, and explicit idempotent initial-user bootstrap. This row is branch evidence only and is not effective on merged main. | Controlled PostgreSQL V8, real mobile Safari/Chrome, real reverse-proxy Session/CSRF, real secret-store injection/rotation, release-owner approval, and P3-H Session/V8 integration remain missing. Production readiness remains BLOCKED. | P3-U1/security targeted tests PASS; full suite 4040 tests, 0 failures, 0 errors, 14 environment-gated skips; disposable localhost two-start H2 authentication/session validation PASS; no Provider/AI/Telegram/trading calls. | Review the P3-U1 Draft PR; P3-U2 remains blocked until reviewed merged-main activation. |
| PDR-1 | Production Config / Profile Lock | DONE | PARTIAL | `application-prod.yml` and `ProductionProfileSafetyGuard` are merged on main. Prod profile rejects H2 memory DB, missing datasource credentials, enabled H2 console, missing/ambiguous production position provider, simulated provider, missing Binance credentials for BINANCE provider, and public bind without explicit opt-in. | This does not prove production deployment readiness. Migration/rollback, PostgreSQL compatibility, auth/authz, deployment smoke/rollback, observability, backup/restore, and secrets ops remain incomplete. | `ProductionProfileSafetyGuardTest` and full Maven suite PASS on merged main. | PDR-2A Database Migration + Rollback Decision Pack |
| PDR-2A | Database Migration + Rollback Decision Pack | DONE | NONE | Docs record PostgreSQL as the target production DB, Flyway as the SQL-first migration framework, forward-only migrations plus pre-migration backup/restore as rollback policy, manual pre-deploy migration as the execution model, RPO 24h / RTO 4h as the initial recovery target, and `schema.sql` as local/test bootstrap for now. | No executable Flyway baseline migration, no PostgreSQL driver/config connection, no schema or mapper SQL changes, no backup/restore commands, no deployment scripts, no auth, no secrets, no production runtime implementation. Production readiness remains BLOCKED. | Docs-only decision pack validation: full Maven PASS, delivery check PASS, v1-state PASS, git diff check PASS when completed. | PDR-2B Flyway Baseline Skeleton, then PDR-2C PostgreSQL Compatibility Pass, then PDR-2D Backup/Restore Runbook |
| PDR-2B | Flyway Baseline Skeleton | DONE | SKELETON | `pom.xml` contains Flyway only inside the explicit non-default `flyway-migration` Maven profile. `src/main/resources/db/migration/README.md` reserves the migration directory. Default local/test H2 + `schema.sql` behavior is preserved. | PostgreSQL baseline schema SQL was deferred to PDR-2C1. No PostgreSQL driver/config connection, no schema or mapper SQL changes, no backup/restore commands, no deployment scripts, no auth, no secrets, no production runtime implementation. Production readiness remains BLOCKED. | Default full Maven validation PASS when completed; delivery check / v1-state run as branch evidence; `git diff --check` PASS. | PDR-2C1 PostgreSQL Baseline Schema SQL, then PDR-2C2 Mapper PostgreSQL Compatibility |
| PDR-2C1 | PostgreSQL Baseline Schema SQL | DONE | SKELETON | `src/main/resources/db/migration/V1__baseline_schema_tables.sql` and `src/main/resources/db/migration/V2__baseline_schema_indexes.sql` add PostgreSQL-compatible baseline table/index SQL for the current 27 V1 tables and 54 existing indexes. JSON-like fields remain TEXT, timestamps remain without timezone, generated ids use identity columns, and V1 remains foreign-key-free. | Files are not yet validated against live PostgreSQL. Mapper PostgreSQL compatibility, PostgreSQL driver/Testcontainers smoke validation, backup/restore commands, deployment scripts, auth, secrets, and production release-gate acceptance remain missing. Production readiness remains BLOCKED. | Default full Maven validation PASS when completed; delivery check / v1-state run as branch evidence; `git diff --check` PASS; incompatible syntax scan over migration files PASS. | PDR-2C2A Mapper PostgreSQL Upsert Variants |
| PDR-2C2A | Mapper PostgreSQL Upsert Variants | DONE | PARTIAL | MyBatis `DatabaseIdProvider` maps PostgreSQL/H2/MySQL database ids. `AssetStateMapper.mergeUpsertCore` and `UserConfigMapper.saveOrUpdate` keep their default H2-compatible generic SQL and add PostgreSQL `ON CONFLICT` upsert variants. | Broader live PostgreSQL mapper execution, backup/restore drill, deployment scripts, auth, secrets, and production release-gate acceptance remain missing. Production readiness remains BLOCKED. | Focused H2 mapper upsert tests PASS; PostgreSQL annotation guard tests PASS; default full Maven validation / delivery check / v1-state run as branch evidence. | PDR-M1 PostgreSQL Runtime Pack |
| PDR-M1 | PostgreSQL Runtime Pack | DONE | PARTIAL | PostgreSQL JDBC runtime dependency, test-scoped Testcontainers/Flyway smoke dependencies, test-only Flyway auto-config disable for default H2 tests, PostgreSQL DATEADD / FORMATDATETIME mapper variants, UserPosition generated-key keyColumn metadata, manual Testcontainers Flyway V1/V2 migration smoke, and backup/restore command templates are present. | Real production PostgreSQL connection, Docker-backed smoke evidence in Docker-less environments, broader live mapper execution, real restore drill, deployment scripts, auth/authz, secrets ops, observability, deployment packaging, deployment smoke/rollback, and explicit production release-gate acceptance remain missing. Production readiness remains BLOCKED. | PostgreSQL/Flyway targeted tests PASS with Docker-unavailable skip for Testcontainers smoke; mapper/static guard tests PASS; full Maven / delivery check / v1-state run as branch evidence. | PDR-M2 Server Deployment + Secrets + Smoke Pack |
| PDR-M2 | Server Deployment + Secrets + Smoke Pack | DONE | SKELETON | `Dockerfile`, `.dockerignore`, `docker-compose.yml`, `.env.example`, `.gitignore` secret/backup ignores, readonly `scripts/prod-smoke.sh`, and PostgreSQL backup/restore template scripts are present. Compose includes PostgreSQL, manual Flyway migration runner profile, and app service with localhost-only host port binding by default. | No real server deployment, no real secrets, no observability, no real restore drill, no secrets manager integration, no external integration readiness, and no production release-gate acceptance. Production readiness remains BLOCKED. | Full Maven validation, delivery check, v1-state, git diff check, shell syntax checks, and Docker Compose config validation run as branch evidence. | PDR-M3 Auth / Access Control Gate, then observability / real deployment smoke |
| PDR-M3 | Auth + Access Control Gate | DONE | PARTIAL | Spring Security Basic Auth protects dashboard/review pages and operational/dashboard/review API surfaces when enabled. `APP_ADMIN_USERNAME` / `APP_ADMIN_PASSWORD` configure the single operator account. Prod profile startup rejects missing admin credentials and unsafe default passwords. `.env.example`, Docker Compose, and readonly smoke script include auth credential handling. | This is not production-ready auth hardening. No database user table, signup, OAuth, role UI, secrets manager integration, HTTPS/reverse proxy, credential rotation, audit logging, rate limiting, real server auth smoke evidence, or production release-gate acceptance exists. Production readiness remains BLOCKED. | Targeted security/auth/production tests PASS as branch evidence; full Maven validation, delivery check, v1-state, git diff check, smoke script syntax check, and Docker Compose config validation run as branch evidence. | PDR-M4 Observability + Production Smoke Gate, then PDR-M5 Real Data Provider Readiness Pack |
| PDR-M4 | Observability + Production Smoke Gate | DONE | PARTIAL | Spring Boot Actuator is present with only `health` exposed. `/actuator/health`, `/actuator/health/liveness`, and `/actuator/health/readiness` are public and hide details/components. `ProductionProfileSafetyGuard` rejects actuator exposure wider than `health`. `prod-smoke.sh` checks public health/liveness/readiness plus authenticated dashboard/review APIs and safety fields. | This is not full production observability. No metrics dashboard, log aggregation, alerting, real server smoke evidence, real restore drill evidence, secrets manager integration, HTTPS/reverse proxy, or production release-gate acceptance exists. Production readiness remains BLOCKED. | Targeted security/health/actuator tests PASS as branch evidence; full Maven validation, delivery check, v1-state, git diff check, smoke script syntax check, and Docker Compose config validation run as branch evidence. | PDR-M5 Real Data Provider Readiness Pack |
| PDR-M5 | Real Data Provider Readiness Pack | DONE | PARTIAL | Readonly provider readiness exists for Binance public market data, AI providers, external context placeholders, dashboard `header.dataSourceText`, and dashboard `diagnostics`. Config-only providers map to `CONFIGURED` / `WAITING_SYNC` / `FAIL_CLOSED`, never fake `CONNECTED`. `ProductionProfileSafetyGuard` rejects explicitly enabled AI providers with missing key/model/base URL. `.env.example` and `prod-smoke.sh` include provider readiness and default `SMOKE_ALLOW_EXTERNAL_CALLS=false`. | This is not live provider verification. No secrets manager, verified live Binance/AI/news/macro/ETF smoke, real server provider evidence, HTTPS/reverse proxy, credential rotation, or production release-gate acceptance exists. Production readiness remains BLOCKED. | Provider readiness, DashboardHomeService, ProductionProfileSafetyGuard, and prod-smoke script tests PASS as branch evidence; full Maven validation, delivery check, v1-state, git diff check, smoke script syntax check, and Docker Compose config validation run as branch evidence. | PDR-M6A Real Server Acceptance Evidence Gate |
| PDR-M6A | Real Server Acceptance Evidence Gate | DONE | PARTIAL | `docs/PRODUCTION_ACCEPTANCE_EVIDENCE_TEMPLATE.md` defines required redacted real-server evidence for Docker Compose config, PostgreSQL startup, Flyway migration, app prod startup, authenticated smoke, backup drill, restore drill, HTTPS/reverse-proxy/auth smoke, provider live smoke, and safety boundary checks. `scripts/prod-release-gate.sh` conservatively orchestrates only safe checks and outputs `PRODUCTION_RELEASE_GATE: PASS / FAIL / INCOMPLETE`; it does not run restore automatically. `.env.example` includes release-gate flags. | No real server deployment, real secrets, completed evidence template, restore drill, HTTPS/reverse-proxy smoke, live provider proof, secrets manager, or human release approval exists. Production readiness remains BLOCKED and is not CONDITIONALLY_READY. | Full Maven validation, delivery check, v1-state, git diff check, shell syntax checks for smoke/backup/restore/release-gate scripts, and Docker Compose config if available. | PDR-M7 Real Provider Live Smoke Harness |
| PDR-M7 | Real Provider Live Smoke Harness | DONE | PARTIAL | `scripts/prod-provider-smoke.sh` adds an opt-in live provider smoke harness. Defaults return `PROVIDER_LIVE_SMOKE: SKIPPED` with no network calls. Binance public market smoke uses a public futures endpoint only. OpenAI/Gemini/XAI smoke checks run only with explicit provider flags and server-side keys. `scripts/prod-release-gate.sh` can require provider smoke only with `RELEASE_GATE_REQUIRE_PROVIDER_SMOKE=true`. `.env.example`, runbook, evidence template, and health tests document and guard the contract. | No real keys, live provider smoke evidence, secrets manager, real server provider evidence, or human release approval exists. Production readiness remains BLOCKED and is not CONDITIONALLY_READY. | Full Maven validation, delivery check, v1-state, git diff check, provider smoke default dry-run, shell syntax checks, and static tests for no Binance order/withdraw endpoint and no obvious secret echo. | User-run real server acceptance and provider live smoke evidence collection; release-gate status closure only after explicit approval. |
| P3-2A | Production Deployment Readiness | BLOCKED | PARTIAL | PDR-1 production profile guard is DONE. PDR-2A database/migration/rollback decisions are recorded. PDR-2B Flyway skeleton is present only behind the explicit non-default `flyway-migration` Maven profile. PDR-2C1 PostgreSQL baseline schema SQL drafts exist. PDR-2C2A mapper upsert variants exist. PDR-M1 PostgreSQL runtime smoke package exists. PDR-M2 Docker Compose deployment skeleton exists. PDR-M3 single-operator Basic Auth gate exists. PDR-M4 minimal health/readiness and smoke gate exists. PDR-M5 readonly provider readiness exists. PDR-M6A real-server evidence gate framework exists. PDR-M7 opt-in provider live smoke harness exists. | Real production DB connection, Docker-backed PostgreSQL smoke evidence where Docker is available, broader live mapper execution, real backup/restore drill, completed real-server evidence template, secrets manager integration, metrics/log aggregation/alerting, real deployment smoke/rollback, HTTPS/reverse-proxy hardening, credential rotation, audit logging, rate limiting, verified external-provider smoke evidence, and explicit production release-gate acceptance remain missing. | PDR-1 tests PASS; PDR-2A docs-only decision evidence; PDR-2B default Maven validation; PDR-2C1 default Maven validation and migration syntax scan evidence; PDR-2C2A focused mapper/config guard tests; PDR-M1 PostgreSQL/static guard tests and full suite evidence; PDR-M2 script/config validation evidence; PDR-M3 targeted security/auth/production tests and full-suite evidence; PDR-M4 targeted health/actuator/security tests and full-suite evidence; PDR-M5 targeted provider/config/production tests and full-suite evidence; PDR-M6A docs/script syntax validation evidence; PDR-M7 provider smoke script/static validation evidence. | Blocks production-ready / conditionally-ready / deployment claim; does not introduce order / execution / auto-trading capability. |
