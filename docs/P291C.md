# BACKEND-P291C Workflow Enforcement and Session Bootstrap Pack

P291C is an A docs/scripts-only pack.

It turns the workflow reset from documentation guidance into executable constraints for new chat windows, Codex tasks, and PR review.

## Baseline

Current merged `main` for this pack:

- `26e8943 BACKEND-P291A Workflow Reset and Progress Source of Truth Pack (#703)`

Current active mainline:

- Market Read Mainline / 行情读取主线
- Active block: MarketReadRequest Test-Only Wiring / 行情读取请求仅测试接线
- Current PR: #705
- Current branch: `p292`

PR #705 is open and does not count as done until merged.

## Added Enforcement

P291C adds:

- session bootstrap instructions;
- machine-readable active mainline status;
- answer format contract;
- pull request template;
- workflow contract check script;
- CI workflow for the contract check.

## Scope Boundary

P291C does not advance P292 or P293.

P291C does not modify Java, tests, DTOs, dashboard, schema, config, service/controller/mapper/repository/scheduler/API code, market clients, runtime data reads, scan output, score, Candidate, Push, Readiness, point generation, entry / stop / TP / RR implementation, order API, execution API, or auto-trading.
