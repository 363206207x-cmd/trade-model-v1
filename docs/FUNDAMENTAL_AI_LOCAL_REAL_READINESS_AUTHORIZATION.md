# Fundamental AI Local-Real Readiness Authorization

Authorization status: `AUTHORIZED_PENDING_MERGED_MAIN`

Exact authorized package:
`LOCAL_REAL_READINESS_SYNC_AND_REAL_ANALYSIS_ENABLEMENT`

Implementation branch:
`codex/local-real-readiness-sync-real-analysis`

Risk: `B_RISK_LOCAL_RUNTIME_DATA_INTEGRITY`

Baseline: `56028b21ac3d4ff9d1ee1368b6a144ad77382e19`

## Authority and Effectivity

This record is subordinate to the sole v4.1 Product Source and the approved
Home interaction contract. It becomes effective only after this exact
authorization is merged to clean, synchronized main and the Product Source,
Workflow and exact-package authorization gates pass.

## Exact Allowed Scope

The successor may:

1. synchronize the existing local-real readiness projection after normal
   manual Asset Pool scans and persisted analysis completion;
2. restore the separately preserved owner-approved current Home route,
   template, stylesheet, runtime binding and directed tests;
3. bind `/dashboard` to current authoritative runtime/database state through
   the existing Dashboard read model and owners;
4. add the minimum ViewModel/read-model mapping required for readiness,
   provider, data quality, alert/event, opportunity, position, Final and
   stored Three-AI state;
5. inspect and use an already supported local real-AI mode only when it exists;
6. run directed compile, regression, authenticated smoke and local browser
   acceptance using existing private configuration without disclosure.

## Forbidden Scope

- no Schema redesign or API-contract redesign;
- no new provider architecture or broad backend refactor;
- no quality-threshold reduction, forced readiness or fake runtime data;
- no Figma, Mobile, unrelated route or Home visual redesign;
- no automatic open, close, add, reduce, reverse, order or trade;
- no secret printing, logging, copying or repository write;
- no global removal of the local-real AI safety guard.

## Machine Permission Contract

Before merge all successor permissions are false. After clean/synchronized
merged-main effectivity, only the exact package resolves to:

```text
REQUESTED_PACKAGE: LOCAL_REAL_READINESS_SYNC_AND_REAL_ANALYSIS_ENABLEMENT
REQUEST_CLASS: AUTHORIZED_IMPLEMENTATION_PACKAGE
REPOSITORY_EDITS_ALLOWED: true
IMPLEMENTATION_ALLOWED: true
PR_CREATION_ALLOWED: true
CANONICAL_FIGMA_DESKTOP_IMPLEMENTATION_ALLOWED: false
MOBILE_IMPLEMENTATION_ALLOWED: false
```

Typos, expanded packages, Schema/provider redesign, Figma, Mobile and
automatic-trading packages fail closed.

## Capability Boundary

- `CAPABILITY_MOVEMENT=NONE`
- `LOCAL_REAL_READINESS_IMPLEMENTATION_STATUS=NOT_STARTED`
- `CURRENT_HOME_RUNTIME_BINDING_STATUS=PRESERVED_NOT_EFFECTIVE`
- `QUALITY_THRESHOLD=UNCHANGED`
- `PRODUCTION_DEPLOYMENT_READINESS=BLOCKED`

