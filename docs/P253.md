# P253 Placeholder

This placeholder exists only to open the Draft PR for BACKEND-P253 ScanScore DTO and Rule Skeleton.

Codex must remove this file and add/update only the authorized files in Issue #624.

Allowed scope: review-only ScanScore DTO / enum / rule skeleton, targeted tests, and docs refresh.

Hard boundaries:

- Do not modify existing Java except allowed score files.
- Do not modify existing tests except allowed score test.
- Do not connect MarketQuoteClient or BinanceMarketQuoteClient.
- Do not read runtime/live/external data.
- Do not connect scheduler.
- Do not create real score computation, scan loop, Candidate, Push, Readiness, point generation, or trading paths.
