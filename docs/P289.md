# P289 MarketReadRequestDTO Closure and Guard Validator Authorization Scope Pack

P289 is an A docs-only Scope Pack for Issue #696.

P289 closes P288 and decides whether the next package may implement a `MarketReadRequestGuardValidator` Java skeleton. P289 itself does not write Java, does not modify tests, and does not modify DTO code.

## 1. P288 Closure

P288 merged as `d837519`.

P288 completed the `MarketReadRequestDTO` pure-data Java skeleton and the targeted DTO test. P288 did not implement real market reads, provider calls, runtime data reads, scan output, score computation, Candidate workflow, Push execution, Readiness, point generation, or trading behavior.

## 2. P289 Scope

P289 only adds docs for:

- P288 `MarketReadRequestDTO` closure;
- future `MarketReadRequestGuardValidator` authorization scope;
- future GuardValidator targeted-test requirements;
- runtime market-read still-blocked status;
- Score / Candidate / Push / Readiness still-blocked status;
- current-state and progress-index updates.

P289 does not modify Java, tests, DTOs, dashboard, schema, config, controller, endpoint, API, service, scheduler, mapper, repository, DB read/write, migration, runtime data, scan output, scan loop, score computation, Candidate production workflow, Push execution, Readiness, point generation, or trading path.

## 3. P290 Authorization Decision

P289 authorizes P290 to implement a `MarketReadRequestGuardValidator` Java skeleton only if P290 remains pure validator only.

If P290 cannot remain inside this validator-only boundary, the recommended next package after P289 must be another docs-only gate instead of Java.

P290, if implemented, may only validate `MarketReadRequestDTO` contract safety and return blocked / review-only / fail-closed validation results. P290 must not connect to service, runtime, market client, provider, scan output, score, Candidate, Push, Readiness, point generation, or trading behavior.

## 4. Future GuardValidator Allowed Scope

Future `MarketReadRequestGuardValidator` may check:

- missing `sourceContractId`;
- missing `watchlistPoolProof`;
- missing `requestedTimeframes`;
- missing `scanTimestamp`;
- missing or invalid `stalePolicy`;
- missing or invalid `missingDataPolicy`;
- `reviewOnly` safety;
- `notTradeInstruction` safety;
- preserved `blockingReasons`;
- preserved `riskBlockers`.

The validator must fail closed and must not upgrade an incomplete or blocked request into a valid market-read action.

## 5. Future GuardValidator Test Requirements

Future P290 targeted tests must cover missing source, missing Watchlist Pool proof, missing requested timeframes, missing scan timestamp, missing or invalid stale policy, missing or invalid missing-data policy, impossible or blocked false review-only / not-trade-instruction paths, preserved blocking reasons, preserved risk blockers, and absence of market client / runtime / scan / score / Candidate / Push / Readiness / point / trading behavior.

## 6. Still Blocked

The following remain blocked after P289:

- `MarketQuoteClient` / `BinanceMarketQuoteClient` wiring;
- runtime/live/external data read;
- provider credentials;
- live provider calls;
- scan output creation;
- real scan loop;
- production ScanScore computation;
- Candidate production workflow;
- Candidate Attention production workflow;
- Promote To Home runtime logic;
- Opportunity Push execution;
- scheduler/API/dashboard-triggered market read;
- external channel behavior / message rendering / sending;
- Telegram / email / webhook / app notification / local notification;
- Readiness / point generation / entry-stop-TP-RR / order / execution / auto-trading.

## 7. Boundary Rules

- Watchlist Pool remains the scan candidate boundary.
- Display Slots / 默认六币 cannot be scan universe, batch universe, push universe, or Watchlist Pool proof source.
- Risk Action Guard must remain before delivery / Push / Readiness.
- 踩踏状态禁止机会推送。
- 插针不等于趋势反转。
- 强反转不等于直接反手。

## 8. Recommended Next Package

Future recommended next package after P289 should be P290 MarketReadRequestGuardValidator Skeleton if the authorization remains complete and validator-only. Otherwise the next package should be another docs-only gate.
