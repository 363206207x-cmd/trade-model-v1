# Dashboard Offline Interaction Acceptance

## Scope and method

This acceptance closes the Dashboard Home interaction semantics on branch `codex/dashboard-state-semantics-audit-fix`. It is offline and read-only:

- Backend states are exercised with deterministic service fixtures and the existing in-memory integration-test database.
- Frontend ownership and interaction order are exercised against the shipped `dashboard.html` source through deterministic call-graph and DOM-target assertions in `DashboardControllerTest`.
- `scripts/dashboard-visual-acceptance-fixture.py` includes `ai-all-abstain` plus verified/unverified position-source scenarios and can generate a self-contained, no-network fixture when localhost binding is unavailable. Reviewer regressions use deterministic fixture/DOM-target assertions; they do not claim a new PNG screenshot.
- No AI/provider call, secret read, scheduler trigger, position mutation, order action, external Push, or Telegram action occurs.

## Scenario acceptance

| Scenario | Operation | Expected | Actual | DOM or test evidence | Result |
|---|---|---|---|---|---|
| AI disabled plus asset directional block | Initial Home load | AI consistency and plan mode are `不适用`; asset directional block remains independently `是`; no synthetic extreme divergence | Home read model separates `aiApplicable=false` from `directionalPushBlocked=true`; disabled role contains no business result | `aiNotApplicableOverridesDirectionalBlockInConsistencyCard`, `disabledAiRoleHasNoBusinessResult`, `headerDisabledAiShowsChineseDisabledLabel` | PASS |
| Three successful AI roles all abstain | Initial Home load and all three role tabs | Header remains `正常`; every role says `复核成功 / 证据不足，暂不判断`; KPI, consistency, conflict, and plan mode are not applicable; score is `--` | Applicability requires support or challenge, not merely a successful call; the exact all-abstain summary is retained | `allThreeSuccessfulAbstainMakeConsistencyNotApplicable`, `allAbstainKpiShowsNotApplicable`, `allAbstainOfflineFixtureUsesNotApplicableDomContract` | PASS |
| AI timeout | Initial Home load and role-tab view | Role shows timeout status and explanation only; no direction, confidence, risk, or AI plan | `resultAvailable=false`; role renderer exits through the unavailable-state panel | `timeoutAiRoleHasNoBusinessResult`, `unavailableAiRoleRendersStatusOnly` | PASS |
| Data quality insufficient | Initial Home load | Final suggestion is fail-closed and contains no plan boundary | Low quality produces `DATA_QUALITY_BLOCKED`; all direction/entry/stop/TP/validity fields are clear | `lowDataQualityAndMissingAnalysisSnapshotHideEveryPlanBoundary` | PASS |
| Plan expired | Initial Home load at before/equal/after expiry clocks | Before expiry is usable; equal/after expiry are blocked and plan fields are clear | Offset-aware `Instant` comparison blocks at `now >= expiresAt` | `expiredAbsoluteValidPeriodBlocksSuggestion`, `offsetAwarePlanExpiryIsTimezoneIndependent` | PASS |
| Asset-state trace differs from decision analysis trace | Initial Home load | Current asset state remains visible; execution plan says `状态已更新，原计划需重新分析` and exposes no boundary | Trace mismatch returns `STATE_SNAPSHOT_MISMATCH` and clears plan fields | `mismatchedAssetStateTraceBlocksPlan` | PASS |
| Position Monitor typed exact plan A through Dashboard | Run Position Monitor, capture its persisted command/log, then load Home with newer plan/decision B available | Monitor logs A only; Home shows only A; B entry/stop/TP/IDs never appear | `EXECUTION_PLAN:<id>` is parsed explicitly, then plan/run/analysis/symbol are verified before trusted IDs are logged; Dashboard consumes that same-position log and exact `ExecutionPlanDO` | `exactPlanA_monitorLogsAAndDashboardShowsOnlyA` | PASS |
| Untyped analysis source with plans A/B through Dashboard | Run Position Monitor with legacy untyped `sourceRefId`, capture log, then load Home | Monitor continues risk/price review but logs `POSITION_SOURCE_UNVERIFIED` and no plan ID; Home displays no original plan and never selects B | No query-order type guessing, no latest-plan query, and Dashboard excludes the structural unverified marker from analysis identity | `positionSourceRefAnalysisWithPlansAAndB_monitorNeverLogsBAsOriginalSource`, `ambiguousSourceRefProducesUnverifiedMonitorSource`, `positionMonitorToDashboardDoesNotReintroduceLatestSiblingFallback` | PASS |
| Monitor source symbol mismatch | Run Position Monitor with typed plan whose AnalysisRun belongs to another symbol | Monitoring log is retained for review but source identity is structural-unverified and plan ID is empty | Symbol mismatch is detected before trusted source fields are written | `monitorSourceSymbolMismatchFailsClosedBeforeLogWrite` | PASS |
| Source plan requires revalidation | Run Position Monitor, capture the persisted log, then load Dashboard Home | Monitor cannot emit `LOGIC_VALID`; it records `LOGIC_WEAKENED`, `RECHECK_PLAN`, and `PLAN_REVALIDATION_REQUIRED`. Dashboard shows the same exact plan only as `REVALIDATION_REQUIRED` historical review | Both components consume `ExecutionPlanReviewPolicy`; populated user stop/take-profit values cannot upgrade the source plan | `needsRevalidationPlanNeverLogsLogicValid`, `positionMonitorAndDashboardAgreeOnRevalidationRequired` | PASS |
| Source plan has missing/placeholder exact boundary | Run Position Monitor, capture the persisted log, then load Dashboard Home | Monitor records `LOGIC_WEAKENED`, `RECHECK_PLAN`, and `PLAN_BOUNDARY_INCOMPLETE`; Dashboard shows `PLAN_INCOMPLETE` and no fabricated boundary | Null, blank, `暂无`, `—`, and `待生成` share one non-concrete-boundary policy across Plan Service, monitor, and Dashboard | `validPlanWithMissingExactBoundaryNeverLogsLogicValid`, `validPlanWithPlaceholderEntryNeverLogsLogicValid`, `validPlanWithPlaceholderStopNeverLogsLogicValid`, `validPlanWithPlaceholderTakeProfitNeverLogsLogicValid`, `placeholderBoundaryVariantsShareOnePolicy`, `positionMonitorAndDashboardAgreeOnPlanIncomplete` | PASS |
| Manual position with exact monitor plan ID among siblings | Initial Home load and position panel view | Position monitoring takes over; exact source plan A is collapsed for historical review; newer same-analysis/same-symbol plan B is absent | `executionPlanId` selects A exactly and must agree with monitor `analysisId`; latest-plan ordering is never used | `monitorExecutionPlanIdStillResolvesExactPlanAmongSiblings`, `monitorExecutionPlanIdAndAnalysisIdMismatchFailsClosed`, `analysisOnlyMultiplePlansDoNotResolveAndExactPlanIdStillReturnsA` | PASS |
| Manual position with typed analysis and exact monitor plan | Position Monitor plus Home read | The monitor-frozen exact plan under `ANALYSIS:<id>` is shown as the source plan | Position Monitor may first prove a unique plan, but Dashboard requires the resulting monitor `analysisId` and exact `executionPlanId`; both must match the current typed position source and plan/run/symbol chain | `typedAnalysisSourceWithSinglePlanLogsVerifiedSource`, `typedAnalysisMonitorExactPlanResolvesOriginalPlan`, `typedAnalysisRequiresMonitorExactPlanAndValidatesItAgainstAnalysis` | PASS |
| Manual position with analysis-only monitor row | Initial Home load and position panel view | Plan area says `暂无可关联的原执行计划`; no sibling/latest plan fields appear | Monitor rows without exact plan ID fail before any unique/latest plan lookup; current typed source cannot turn a historical generic row into verified identity | `analysisOnlyWithMultiplePlansFailsClosed`, `analysisOnlyWithNoPlanFailsClosed`, `analysisOnlyMultiplePlansNeverSelectsLatestSibling` | PASS |
| Verified but inactive original plan | Initial Home load and collapsed historical plan review | Exact plan A remains visible only as historical review with the backend label; plan B and executable status remain absent | Current validity reads `ExecutionPlanDO` status/source gate/revalidation first, then state trace and decision time; frontend checks only `originalPlanIdentity` and uses `originalPlanLabel` | `invalidOriginalPlanIsNotMarkedActive`, `blockedOriginalPlanIsNotMarkedActive`, `incompleteOriginalPlanIsNotMarkedActive`, `sourceGateIncompleteOriginalPlanIsNotMarkedActive`, `sourceGateCompleteFalseOriginalPlanIsNotMarkedActive`, `needsRevalidationOriginalPlanIsNotMarkedActive`, `hotResetRevalidationReasonIsPreservedAsReviewCopy`, `executionPlanStatusAndDecisionExpiryUseDeterministicPriority`, `verifiedOriginalPlanUsesBackendIdentityAndLabelWithoutInferringValidity` | PASS |
| `VALID` original plan with missing/placeholder exact boundary | Initial Home load and collapsed historical plan review | Current validity is `PLAN_INCOMPLETE`; missing values remain blank and sibling decision fields are not borrowed | Exact `ExecutionPlanDO.entryZone/stopLoss/takeProfitRules` is authoritative; null/blank/`暂无`/`—`/`待生成` fails closed before trace/time ACTIVE checks | `validStatusWithMissingEntryIsPlanIncomplete`, `validStatusWithMissingStopIsPlanIncomplete`, `validStatusWithMissingTakeProfitIsPlanIncomplete`, `placeholderBoundariesArePlanIncomplete`, `incompletePlanANeverBorrowsBoundariesFromPlanB`, `validSourceTraceWithoutReadyBoundaryProducerCannotRemainBoundaryComplete` | PASS |
| Revalidation reason copy | Initial Home load for known, unknown, mixed-code, JSON, and Hot Reset reasons | Only fixed Chinese whitelist labels or generic Chinese fallback appear; no internal enum/JSON/raw reason leaks | Backend maps four known reason codes and never echoes arbitrary reason content | `hotResetFallbackUsesChineseOnly`, `mixedChineseAndInternalCodeDoesNotLeak`, `unknownRevalidationReasonUsesGenericChineseCopy`, `knownRevalidationReasonsUseWhitelistLabels` | PASS |
| Manual position without a verifiable plan source | Initial Home load and position panel view | Monitoring fields remain available, but the plan area says `暂无可关联的原执行计划`; no empty disclosure or plan B fields appear | No monitor source means `originalPlanIdentity=UNVERIFIED`; ambiguous `sourceRefId` is diagnostic-only and no latest-symbol fallback runs | `positionWithNoSourceReferenceHidesOriginalPlan`, `activePositionNeverFallsBackToLatestSymbolDecision`, `unverifiedPositionSourceDoesNotRenderEmptyOriginalPlanTable` | PASS |
| Source plan belongs to another symbol | Initial Home load | Original plan fails closed and no current/new same-symbol plan replaces it | Exact source identity requires position, decision, and analysis-run symbols to match | `originalPlanSymbolMismatchFailsClosed` | PASS |
| Source plan A trace differs from current state B | Initial Home load | Plan A may remain as an explicitly stale historical review record; plan B is never promoted | Identity stays `VERIFIED`; current validity becomes `STATE_MISMATCH` with `状态已更新，原计划不再作为当前执行依据` | `sourceTraceMismatchDoesNotPromoteNewDecision` | PASS |
| Active monitor to closed-position review | Open a manual fixture position from exact typed plan A or typed analysis X, freeze exact monitor plan A, manually close, then add sibling B | Closed review chooses the newest trusted monitor row and retains A; B is never selected. Summary and feedback consume the same resolved context | `PositionPlanSourceResolver` validates monitor IDs against current typed source; only if no trusted monitor row exists may review fall back to typed position identity | `activeMonitorAndClosedReviewUseSameSourceResolution`, `exactPlanASurvivesMonitorCloseAndReviewWithoutPlanBSubstitution`, `typedAnalysisMonitorsA_thenSiblingBAppears_closedReviewStillUsesA`, `closedReviewAndFeedbackUseSameMonitorResolvedPlanA` | PASS |
| Closed position has an unverifiable plan source | Build summary/feedback for wrong-symbol, missing-run, run-mismatch, or ambiguous-analysis source | Plan fields are empty, deviation is `NOT_COMPUTABLE`, reason includes `PLAN_SOURCE_UNVERIFIED`, and feedback uses `USER_POSITION_<id>` | Shared resolver fails closed before plan display, deviation, or analysis feedback binding | `closedPositionCrossSymbolTypedPlanFailsClosed`, `closedPositionMissingAnalysisRunFailsClosed`, `closedPositionPlanRunAnalysisMismatchFailsClosed`, `closedPositionAmbiguousAnalysisSourceFailsClosed`, `unverifiedClosedPositionFeedbackNeverTargetsForeignAnalysis` | PASS |
| Generic or historical monitor source reaches user boundary | Read sentinel, non-sentinel legacy, typed mismatch, and resolver-backed monitor rows | Sentinel and failed business joins expose null IDs plus `UNVERIFIED`; generic non-sentinel API rows remain `PENDING_VERIFICATION`; only resolver-backed business rows become `VERIFIED` | `PositionMonitorLogSourceViewPolicy` no longer treats non-sentinel as proof. Dashboard/closed review compare monitor IDs with current typed position source before marking verified | `unverifiedMonitorLogDtoHidesInternalSentinel`, `legacyNonSentinelMonitorRowIsNotAutomaticallyVerified`, `genericMonitorLogApiDoesNotClaimVerifiedWithoutResolverEvidence`, `legacyGuessedSiblingBDoesNotReachDashboardOrReviewCenter`, `trustedExactPlanMonitorSourceCanBeMarkedVerified`, `visibleDomContainsNoPositionSourceUnverified` | PASS |
| Manual position without monitor record | Initial Home load and position panel view | Monitoring fields say `等待首次监控`; no monitor result or next time is fabricated | No log maps to `WAITING_MONITOR`; a prior log with no next schedule instead says `暂无下次监控排期` | `existingMonitorWithoutNextScheduleDoesNotSayFirstMonitor` | PASS |
| Backend `DEFAULT_SLOT` asset | Initial Home load, then attempted tile/sidebar click | Placeholder says `等待首轮分析`, is noninteractive, and starts no detail/Home selection | Both tile and sidebar identify `slotType=DEFAULT_SLOT`, omit a selectable symbol, and reject click handling | `defaultSlotIsNonInteractivePlaceholder` | PASS |
| Home API failure | Refresh | Cached position/plan/AI conclusions are cleared; main surface says `首页数据暂不可用`, `等待重新同步`, `当前不展示执行计划` | Failure path calls only the unavailable Home renderer plus diagnostics refresh; summary cannot reconstruct Home | `homeApiFailureRendersFailClosedEmptyState`, `legacyHomeRenderersAreNotCalledByCurrentHomeFlow` | PASS |

## Interaction-order acceptance

| Operation | Expected | Actual | DOM or test evidence | Result |
|---|---|---|---|---|
| First load | Fetch Home, render all main semantics once, then request selected-symbol detail | `refreshDashboard -> fetchDashboardHome -> renderDashboardHomePayload`; detail is requested only after Home resolves | `legacyHomeRenderersAreNotCalledByCurrentHomeFlow`, `detailResponseCannotOverwriteHomeSemanticSections` | PASS |
| Click asset tile | Normalize symbol, assign `selectedSymbol`, refresh Home | Click handler assigns symbol before `refreshDashboard()` | `sidebarSelectionRefreshesDashboardHome` | PASS |
| Click sidebar asset | Normalize symbol, assign `selectedSymbol`, refresh Home | Sidebar handler assigns symbol before `refreshDashboard()` | `sidebarSelectionRefreshesDashboardHome` | PASS |
| Select search result | Normalize symbol, assign `selectedSymbol`, refresh Home | Search selection assigns symbol before `refreshDashboard()` | `sidebarSelectionRefreshesDashboardHome` | PASS |
| Refresh page | Home remains the sole successful main renderer | No summary/detail renderer is reachable as a Home business renderer | `legacyHomeRenderersAreNotCalledByCurrentHomeFlow` | PASS |
| Detail response arrives after Home | Home position, execution, AI role, consistency, tiles, and KPIs remain unchanged | Detail call graph updates only status cards, workbench, detail sections, and diagnostics | `detailResponseCannotOverwriteHomeSemanticSections` | PASS |
| Home request fails | Fail closed without a legacy detail/summary plan fallback | Home caches are cleared and diagnostics refresh has no Home semantic renderer | `homeApiFailureRendersFailClosedEmptyState` | PASS |

## Decision

Offline Dashboard interaction acceptance: **PASS** for the scoped semantic contract.

Plan `validFrom`/`expiresAt` are offset-aware authority. The historical Push Snapshot timestamp remains no-timezone storage, but its production and consumption contract is explicitly UTC-naive. AI call success does not establish consistency applicability, `ABSTAIN` is neither support nor objection, and the AI conflict KPI is derived from the same backend consistency object as the card. In position mode, latest-symbol decision B is never an identity fallback for source plan A. Position Monitor, Dashboard, and closed-position review share one source resolver; monitor-derived identity additionally requires exact monitor IDs to match the current typed position source. Closed review preserves monitor-frozen A when sibling B appears later, and summary/feedback share that resolution. Generic monitor rows remain pending rather than self-verifying. Revalidation and incomplete exact boundaries remain manual recheck states even when user stops/targets are populated. The internal unverified-source sentinel is retained only for persistence and is absent from user DTO/API/DOM output.

This is not production acceptance. The eight-state lifecycle remains incomplete, the three-bar-per-timeframe market-bias policy remains provisional, Flyway V7 still needs separate controlled PostgreSQL evidence, and production deployment readiness remains **BLOCKED**.
