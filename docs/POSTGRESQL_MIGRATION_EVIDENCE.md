# PostgreSQL Migration Evidence

Package: PDR-PF3 PostgreSQL Migration Evidence
Date: 2026-07-07
Branch: `codex/pdr-pf3-postgresql-migration-evidence`
Base main commit reviewed: `4816ec62c783acba44615e38494e394d663fd364` or newer
Production readiness: BLOCKED
Production deployment: cannot proceed
Empty PostgreSQL migration result: BLOCKED_TIMEOUT

## Scope

This is a production-readiness evidence package only. It records migration evidence status after an interrupted PostgreSQL migration evidence run. It does not modify business runtime behavior, trading logic, order execution, scheduler execution, Push/Telegram sending, dashboard behavior, schema SQL, or Flyway migration SQL.

## Migration Files Reviewed

| File | Purpose | Review result |
|---|---|---|
| `src/main/resources/db/migration/V1__baseline_schema_tables.sql` | PostgreSQL baseline table creation. | Reviewed. Uses PostgreSQL-compatible table DDL and `GENERATED BY DEFAULT AS IDENTITY` for identity keys. |
| `src/main/resources/db/migration/V2__baseline_schema_indexes.sql` | PostgreSQL baseline indexes and unique indexes. | Reviewed. Uses `CREATE INDEX IF NOT EXISTS`, which is supported by PostgreSQL. |
| `src/main/resources/db/migration/V3__scheme_rule_config_defaults.sql` | Scheme rule-config default rows. | Reviewed. Uses PostgreSQL `ON CONFLICT (rule_key) DO UPDATE`. |

Static scan command:

```bash
rg -n "AUTO_INCREMENT|MERGE INTO|ON DUPLICATE|DATEADD|FORMATDATETIME|CLOB" src/main/resources/db/migration/*.sql
```

Static scan result: no matches in Flyway migration SQL.

## schema.sql Compatibility Review

`src/main/resources/schema.sql` remains the H2 local/test bootstrap file. It is not the production Flyway migration source.

Static scan found H2/local bootstrap syntax in `schema.sql`, including:

- `CLOB`
- `AUTO_INCREMENT`
- `MERGE INTO tm_rule_config KEY(rule_key)`

These are expected in the existing local/test bootstrap path and are not copied into the PostgreSQL Flyway files. The PostgreSQL migration path uses the dedicated files under `src/main/resources/db/migration/`.

## Empty PostgreSQL Migration Result

Result: BLOCKED_TIMEOUT

Command / path under evaluation:

```bash
./mvnw -q -Dtest=PostgreSqlFlywayMigrationSmokeTest test
```

Timeout / interruption evidence:

- The previous PDR-PF3 PostgreSQL migration evidence run lasted approximately 1h27m before manual interruption.
- It did not produce a completed, trustworthy migration log proving successful Flyway V1/V2/V3 execution against PostgreSQL.
- Per the follow-up task, the PostgreSQL migration smoke was not rerun.
- No single long Docker/Testcontainers/PostgreSQL/Maven command was rerun for this finalization pass.

Reason: Docker/Testcontainers/PostgreSQL migration smoke did not complete in acceptable time. Empty PostgreSQL migration success remains unproven.

Local Docker probe evidence from the earlier investigation:

```bash
docker info
```

Result:

```text
zsh:1: command not found: docker
```

Existing local Surefire skip evidence from the earlier investigation:

```text
Tests run: 1, Failures: 0, Errors: 0, Skipped: 1
Skipped reason: Docker/Testcontainers is unavailable; PostgreSQL smoke skipped
Elapsed: 0.125 s
```

This skip evidence confirms that the local environment cannot currently prove PostgreSQL migration success. It is not a successful empty PostgreSQL migration proof.

Testcontainers output summary from the earlier investigation:

```text
Could not find a valid Docker environment.
UnixSocketClientProviderStrategy: NoSuchFileException (/var/run/docker.sock)
DockerDesktopClientProviderStrategy: socket path unavailable
```

A timeout-wrapped rerun was not executed during this finalization pass. The accepted evidence status is therefore `BLOCKED_TIMEOUT`, not PASS.

## No Production Database Impact

- No production database was accessed.
- No production secrets were used.
- No destructive database operation was run.
- No backup, restore, migration, rollback, or DDL operation was executed against a production database.

## What The Smoke Would Prove If Docker Were Available

`PostgreSqlFlywayMigrationSmokeTest` is designed to:

1. Start `postgres:16-alpine` through Testcontainers.
2. Run Flyway against `classpath:db/migration`.
3. Verify 27 `tm_%` baseline tables exist.
4. Verify critical tables such as `tm_analysis_run`, `tm_decision_result`, `tm_execution_plan`, `tm_user_position`, `tm_position_monitor_log`, `tm_review_result`, `tm_opportunity_log`, `tm_push_snapshot`, `tm_push_recheck_log`, `tm_ai_call_log`, and `tm_asset_state`.
5. Verify representative indexes exist.
6. Verify `flyway_schema_history` has successful migrations.
7. Verify PostgreSQL identity generated keys for `tm_user_position`.

Because the migration evidence run timed out / was interrupted and no completed PostgreSQL migration success log exists, this package cannot prove V1/V2/V3 migration success.

## Remaining Blockers

1. Empty PostgreSQL Flyway migration is not proven in this local environment because the Docker/Testcontainers/PostgreSQL migration evidence did not complete within an acceptable time and was manually interrupted.
2. No real PostgreSQL server migration evidence is attached.
3. No migration-from-current-main/live-state rehearsal exists.
4. No backup/restore drill evidence exists.
5. No rollback verification evidence exists.
6. No completed real-server production release-gate evidence exists.
7. Production secrets, live server access, and real provider evidence remain outside this package.

## Production Readiness Decision

Production readiness remains BLOCKED.

Production deployment cannot proceed.

This package records PostgreSQL migration evidence status as `BLOCKED_TIMEOUT`. It does not prove production-ready deployment because empty PostgreSQL migration did not run to completion, and current-state migration plus rollback/restore evidence remains missing.

## Recommendation

Recommended next remediation:

1. First resolve Docker/Testcontainers availability or use a controlled server-backed PostgreSQL test environment.
2. Re-run PDR-PF3 in that environment and capture successful Flyway V1/V2/V3 evidence.
3. Only after empty PostgreSQL migration evidence is PASS, continue to `PDR-PF4 Current-State Migration + Rollback Drill`.

## Prohibited Items Preserved

- no auto-open
- no auto-close
- no auto-reverse
- no order execution
- no auto-trading
- no external push send
- no fake positions
- no fake review records
- no production-ready claim
