# P276 Real Scan Score Candidate Return Scope Pack

P276 is a docs-only Scope Pack after P275.

P276 redirects the project mainline away from further external-channel no-op expansion and back toward planning for real scan, real score, Candidate, and Push preview.

P276 does not write Java, tests, DTOs, dashboard code, schema, config, API, scheduler, MarketQuoteClient wiring, BinanceMarketQuoteClient wiring, runtime/live/external data reads, real scan loops, production ScanScore computation, Candidate production workflow, Push execution, Readiness, point generation, entry/stop/TP/RR, order/execution, or auto-trading.

## Baseline

Current main before P276:

```text
2628f8c BACKEND-P275 External Channel Closure and CI Exception Record Scope Pack (#669)
```

P275 merged as `2628f8c`.

P274 was a local-validation exception merge, and P275 recorded it. The P274 exception record remains narrow: GitHub CI did not trigger for PR #667 / HEAD `929841d`, an empty retrigger commit was attempted, Ready for review transition was attempted, local targeted test / compile / test-compile passed, the user explicitly approved the local-validation exception merge, and P274 was merged as `905fb41`.

## Purpose

P276 stops treating external-channel no-op expansion as the immediate next direction.

The next recommended project direction returns to:

- real scan planning
- real score planning
- Candidate planning
- Push preview planning

This is planning only. P276 does not implement real scan, real score, Candidate production, Push preview runtime behavior, or external push execution.

## P276 Scope Documents

P276 adds:

- `PHASE_P276_REAL_SCAN_RETURN_SCOPE.md`
- `PHASE_P276_REAL_SCORE_RETURN_SCOPE.md`
- `PHASE_P276_CANDIDATE_RETURN_SCOPE.md`
- `PHASE_P276_PUSH_PREVIEW_RETURN_SCOPE.md`
- `PHASE_P276_RUNTIME_MARKET_READ_STILL_BLOCKED.md`
- `PHASE_P276_READINESS_POINT_GENERATION_STILL_BLOCKED.md`

P276 also updates:

- `V1_CURRENT_STATE.md`
- `PROJECT_PROGRESS_INDEX.md`

## Real Scan Return

Real scan is only a planning topic in P276.

The future plan may audit scan contracts, Watchlist Pool boundaries, market-read boundaries, scan output shape, and fail-closed semantics. P276 does not connect `MarketQuoteClient`, does not connect `BinanceMarketQuoteClient`, does not read runtime/live/external data, and does not create a real scan loop.

Display Slots / 默认六币 cannot become the batch universe. Watchlist Pool remains the candidate boundary.

## Real Score Return

Real score is only a planning topic in P276.

The future plan may audit score inputs, score ownership, incomplete data behavior, review-only states, and Risk Action Guard preconditions. P276 does not implement production ScanScore computation and does not upgrade any candidate into readiness.

## Candidate Return

Candidate production is only a planning topic in P276.

The future plan may audit Candidate lifecycle boundaries, attention semantics, Promote To Home preview semantics, and fail-closed review states. P276 does not implement Candidate production workflow and does not implement Promote To Home runtime logic.

## Push Preview Return

Push preview is only a planning topic in P276.

Push preview means internal review-only preview planning. It is not Opportunity Push execution, not external push execution, not message rendering, not message sending, and not provider or external channel behavior.

Risk Action Guard must remain before any delivery or preview escalation. Stampede state blocks opportunity push. A wick-only/pin-bar move is not a trend reversal. Strong reversal is not direct reverse trading.

## Still Blocked

The following remain blocked:

- `MarketQuoteClient` wiring
- `BinanceMarketQuoteClient` wiring
- runtime/live/external data reads
- real scan loop
- production ScanScore computation
- Candidate production workflow
- Promote To Home runtime logic
- Opportunity Push execution
- scheduler/API/dashboard wiring
- external channel behavior
- provider credentials
- live provider calls
- message rendering
- message sending
- Telegram/email/webhook/app notification/local notification
- Readiness
- point generation
- real entry/stop/TP/RR
- order API
- execution API
- auto-trading

## Recommended Next Package

The recommended next package after P276 should be a real scan contract audit / market-read boundary plan, not implementation.

That package should remain docs-only unless a later issue explicitly authorizes Java, market reads, runtime data access, scan loop behavior, or production score computation.

## Verification

P276 is docs-only, so Maven may be skipped unless Java/test/resources are modified.

Required checks:

```bash
git diff --name-status main...HEAD
git diff --check
git status
```
