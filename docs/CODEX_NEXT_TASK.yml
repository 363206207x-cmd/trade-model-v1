compatibility_status: "DERIVED_ONLY"
active_block: "PDR-PF10 PostgreSQL Environment Provisioning Evidence"
current_phase: "P0-0"
current_phase_status: "DONE"
completion_effective_state: "DERIVED_BY_V1_STATE"
existing_module_maturity: "PARTIAL"
module: "PDR-PF10 PostgreSQL Environment Provisioning Evidence"
branch: "codex/pdr-pf10-postgresql-environment-provisioning"
risk: "B-risk production-readiness environment evidence package"
current_pr: "none"
next_business_phase: "Post-freeze user acceptance / production readiness remediation"
next_business_phase_allowed: false
compatibility_next_business_phase_allowed_note: "Compatibility field remains false until this PostgreSQL environment provisioning evidence package is merged/effective; runtime v1-state derives actual next-phase allowance on clean synced main."
next_allowed_action: "Complete PDR-PF10 PostgreSQL Environment Provisioning Evidence. Check Docker/Testcontainers environment readiness, run PostgreSQL smoke only if Docker is available, and keep production deployment disallowed."
current_package_pr: "none"
unrelated_open_prs: "DERIVED_BY_V1_STATE"
block_next_business_phase_only: false
a_risk_auto_merge_rule: "enabled for docs / contract / workflow packages only"

allowed_scope:
  - "Create docs/POSTGRESQL_ENVIRONMENT_PROVISIONING_EVIDENCE.md"
  - "Check Docker CLI, Docker daemon, and Docker socket availability"
  - "Check existing PostgreSQL/Flyway smoke test presence"
  - "Run targeted PostgreSQL smoke only if Docker/Testcontainers availability is confirmed"
  - "Record DOCKER_AVAILABLE, DOCKER_MISSING, DOCKER_DAEMON_UNAVAILABLE, DOCKER_SOCKET_UNAVAILABLE, or BLOCKED_ENV_TIMEOUT honestly"
  - "Update production-readiness docs and status-source docs for PF10"
forbidden_scope:
  - "Java business logic"
  - "schema.sql"
  - "Flyway SQL migration changes unless a real compatibility bug is proven and explicitly scoped"
  - "application.yml or application-prod.yml"
  - "production DB access"
  - "real secrets"
  - "destructive DB operations"
  - "order execution"
  - "auto-open"
  - "auto-close"
  - "auto-reverse"
  - "auto-trading"
  - "external push send"
  - "fake positions"
  - "fake review records"
  - "production-ready claim"
checks:
  - "targeted PostgreSQL smoke only if Docker/Testcontainers availability is confirmed"
  - "git diff --check"
  - "bash scripts/check-workflow-contract.sh"
  - "bash scripts/v1-delivery-check.sh"
  - "bash scripts/v1-state.sh"
  - "YAML status files parse successfully"
stop_conditions:
  - "any single command would run longer than 10 minutes"
  - "change requires production DB access, real secrets, or destructive DB operation"
  - "change would claim production readiness without explicit PASS evidence for every production gate"
  - "change requires order execution, auto-open, auto-close, auto-reverse, auto-trading, or external push send"
  - "change requires fake positions or fake review records"
  - "delivery check or v1-state reports a blocker unrelated to the current unmerged branch"
