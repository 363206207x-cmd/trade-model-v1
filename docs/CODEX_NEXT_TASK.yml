compatibility_status: "DERIVED_ONLY"
active_block: "P3-U1 personal login and server-side Session authentication are locally validated but remain unmerged"
current_phase: "P0-0"
current_phase_status: "DONE"
completion_effective_state: "DERIVED_BY_V1_STATE"
existing_module_maturity: "PARTIAL"
module: "P3-U1 Personal Login Page and Session Authentication"
branch: "codex/p3-u1-personal-login-session-auth"
risk: "B-risk authentication, Session, CSRF, tm_user V8 migration, bootstrap, tests, and directly related configuration; no external calls or trading"
current_pr: "PR #1133 OPEN / DRAFT / UNMERGED"
next_business_phase: "P3-U2 Mobile Dashboard Productization"
next_business_phase_allowed: false
compatibility_next_business_phase_allowed_note: "P3-U1 must be independently reviewed and effective on merged main before P3-U2; P4 and production deployment remain blocked."
next_allowed_action: "Review the P3-U1 Draft PR, exact diff, tests, localhost two-start evidence, and remaining PostgreSQL/mobile/deployment gates."
current_package_pr: "P3-U1 login and Session authentication; LOCAL_IMPLEMENTATION_VALIDATED / PR_OPEN_UNMERGED"
unrelated_open_prs: "DERIVED_BY_V1_STATE"
block_next_business_phase_only: false
a_risk_auto_merge_rule: "not applicable; P3-U1 is B-risk Java/security/schema work"
production_deployment_readiness: "BLOCKED"

allowed_scope:
  - "Review GET/POST /login, POST /logout, browser redirect, and sanitized API 401 behavior"
  - "Review Spring Security server-side Session, fixation migration, Cookie, timeout, and CSRF contracts"
  - "Review tm_user H2 schema and PostgreSQL V8 migration"
  - "Review BCrypt user lookup, last_login_at update, fail-closed database errors, and sanitized audit"
  - "Review bounded single-instance login failure limiting and reset/expiry behavior"
  - "Review explicit, idempotent, no-overwrite initial-user bootstrap"
  - "Review login-page responsive/static accessibility and existing browser-write CSRF headers"
  - "Review directly related root Compose and P3-H ConfigTree authentication property alignment"
  - "Review targeted/full tests and localhost disposable H2 two-start evidence"
  - "Keep controlled PostgreSQL V8, real mobile, real staging, and production readiness explicitly unproven"
forbidden_scope:
  - "P3-U2 Dashboard redesign or mobile Dashboard productization"
  - "multi-user administration, RBAC, OAuth, registration, password recovery, JWT, refresh token, or custom token systems"
  - "real Provider, AI, Binance private API, Telegram, Push, webhook, email, or other external calls"
  - "business scheduler activation"
  - "production server or production database access"
  - "real secret read, committed secret, committed .env, or secret output"
  - "destructive database operation"
  - "order execution, auto-open, auto-close, auto-reverse, auto-trading, or position mutation"
  - "P4"
  - "production-ready claim"
checks:
  - "./mvnw test -q"
  - "git diff --check"
  - "bash scripts/check-workflow-contract.sh"
  - "bash scripts/v1-delivery-check.sh"
  - "bash scripts/v1-state.sh"
  - "YAML status files parse successfully"
  - "bash -n deploy/p3h/p3h-secret-materializer.sh"
stop_conditions:
  - "P3-U1 exact branch changes after review"
  - "change requires P3-U2, Provider/AI architecture, trading behavior, or a production deployment"
  - "change requires real credentials, real external calls, production DB access, or destructive operations"
  - "test failure can only be hidden by weakening security or disabling assertions"
  - "request would mark PostgreSQL V8, real mobile, real staging, or production readiness PASS without actual evidence"
