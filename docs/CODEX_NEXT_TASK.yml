compatibility_status: "DERIVED_ONLY"
active_block: "PDR-LIVE2 Controlled PostgreSQL Evidence Setup"
current_phase: "P0-0"
current_phase_status: "DONE"
completion_effective_state: "DERIVED_BY_V1_STATE"
existing_module_maturity: "PARTIAL"
module: "PDR-LIVE2 Controlled PostgreSQL Evidence Setup"
branch: "codex/pdr-live2-controlled-postgresql-evidence-setup"
risk: "B-risk controlled PostgreSQL evidence setup package"
current_pr: "none"
next_business_phase: "Post-freeze user acceptance / production readiness remediation"
next_business_phase_allowed: false
compatibility_next_business_phase_allowed_note: "Compatibility field remains false until this controlled PostgreSQL evidence setup package is merged/effective; runtime v1-state derives actual next-phase allowance on clean synced main."
next_allowed_action: "Complete PDR-LIVE2 Controlled PostgreSQL Evidence Setup. Define the safe path to PostgreSQL Flyway PASS evidence without production DB access, destructive operations, secret disclosure, or production deployment approval."
current_package_pr: "none"
unrelated_open_prs: "DERIVED_BY_V1_STATE"
block_next_business_phase_only: false
a_risk_auto_merge_rule: "enabled for docs / contract / workflow packages only"

allowed_scope:
  - "Create docs/CONTROLLED_POSTGRESQL_EVIDENCE_SETUP.md"
  - "Add safe no-op/dry-run controlled PostgreSQL setup helper if useful"
  - "Check controlled DB env presence without printing values"
  - "Check Docker availability without running long Docker/Testcontainers commands"
  - "Document safe bounded command plan for Docker/Testcontainers and controlled external PostgreSQL paths"
  - "Update production-readiness docs and status-source docs for LIVE2"
forbidden_scope:
  - "production DB access"
  - "destructive DB operations"
  - "real secrets"
  - "committed .env"
  - "printing secret values"
  - "Java business logic"
  - "schema.sql"
  - "Flyway SQL migration changes"
  - "order execution"
  - "auto-open"
  - "auto-close"
  - "auto-reverse"
  - "auto-trading"
  - "external push send"
  - "fake positions"
  - "fake review records"
  - "production-ready claim"
checks:
  - "git diff --check"
  - "bash scripts/check-workflow-contract.sh"
  - "bash scripts/v1-delivery-check.sh"
  - "bash scripts/v1-state.sh"
  - "bash -n any new/changed scripts"
  - "YAML status files parse successfully"
stop_conditions:
  - "any single command would run longer than 5 minutes"
  - "change requires production DB access, real secrets, secret printing, or destructive DB operation"
  - "change would claim production readiness without explicit PASS evidence for every production gate"
  - "change requires order execution, auto-open, auto-close, auto-reverse, auto-trading, or external push send"
  - "change requires fake positions or fake review records"
  - "delivery check or v1-state reports a blocker unrelated to the current unmerged branch"
