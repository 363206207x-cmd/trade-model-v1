compatibility_status: "DERIVED_ONLY"
active_block: "P2-2 AI Orchestrator + AiCallLog PR merge gate"
current_phase: "P0-0"
current_phase_status: "DONE"
completion_effective_state: "DERIVED_BY_V1_STATE"
existing_module_maturity: "PARTIAL"
module: "AI Orchestrator + AiCallLog"
branch: "p2-2-ai-orchestrator-ai-call-log-full-implementation"
risk: "B"
next_business_phase: "P2-3 Scheduler / Idempotency / Trace"
next_business_phase_allowed: false
next_allowed_action: "Review and merge-gate the P2-2 AI Orchestrator + AiCallLog B-risk PR. P2-3 remains blocked until this PR is reviewed, merged into main, local main is synced, the worktree is clean, and runtime state confirms P2-2 effective."
current_package_pr: "DERIVED_BY_V1_STATE"
unrelated_open_prs: "DERIVED_BY_V1_STATE"
block_next_business_phase_only: false
a_risk_auto_merge_rule: "enabled for docs / contract / workflow packages only"

allowed_scope:
  - "AiDecisionOrchestratorService"
  - "AiProviderClient abstraction"
  - "OpenAI / GPT safe adapter"
  - "Gemini safe adapter"
  - "xAI / Grok safe adapter"
  - "AiUsageGuard budget and rate-limit fail-closed guard"
  - "tm_ai_call_log / AiCallLogDO / AiCallLogMapper / AiCallLogService"
  - "DecisionEngine review-only AI orchestration integration"
  - "Read-only AI status and call-log APIs"
  - "P2-2 tests and documentation"

forbidden_scope:
  - "P2-3 Scheduler / Idempotency / Trace"
  - "Dashboard UI"
  - "provider refresh / external fetch endpoint"
  - "Push send"
  - "external channel"
  - "order / execution / auto-trading"
  - "UserPosition creation / mutation"
  - "ExecutionPlan creation"
  - "AI direction override"
  - "state-machine override"
  - "merge this B-risk PR without review confirmation"

checks:
  - "./mvnw test -q"
  - "bash -n scripts/check-workflow-contract.sh"
  - "bash -n scripts/v1-state.sh"
  - "bash -n scripts/v1-auto.sh"
  - "bash -n scripts/codex-next-task.sh"
  - "bash scripts/check-workflow-contract.sh"
  - "bash scripts/codex-next-task.sh --validate"
  - "bash scripts/v1-state.sh"
  - "bash scripts/v1-auto.sh summary"
  - "bash scripts/v1-auto.sh next"
  - "git diff --check"

stop_conditions:
  - "P2-3 task generation before P2-2 merged-main effectivity"
  - "forbidden business surface change"
  - "workflow contract failure"
  - "Maven failure"
