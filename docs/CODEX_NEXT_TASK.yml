compatibility_status: "DERIVED_ONLY"
active_block: "PDR-LIVE3 Controlled PostgreSQL Flyway Smoke Runner"
current_phase: "P0-0"
current_phase_status: "DONE"
completion_effective_state: "DERIVED_BY_V1_STATE"
existing_module_maturity: "PARTIAL"
module: "PDR-LIVE3 Controlled PostgreSQL Flyway Smoke Runner"
branch: "codex/pdr-live3-controlled-postgresql-flyway-smoke-runner"
risk: "B-risk controlled PostgreSQL Flyway smoke runner package"
current_pr: "none"
next_business_phase: "Post-freeze user acceptance / production readiness remediation"
next_business_phase_allowed: false
compatibility_next_business_phase_allowed_note: "Compatibility field remains false until this controlled PostgreSQL Flyway smoke runner package is merged/effective; runtime v1-state derives actual next-phase allowance on clean synced main."
next_allowed_action: "Complete PDR-LIVE3 Controlled PostgreSQL Flyway Smoke Runner. Add the guarded bounded runner and skip safely when controlled DB env is missing; do not access production DB, run destructive operations, print secrets, or approve production deployment."
current_package_pr: "none"
unrelated_open_prs: "DERIVED_BY_V1_STATE"
block_next_business_phase_only: false
a_risk_auto_merge_rule: "enabled for docs / contract / workflow packages only"

allowed_scope:
  - "Create docs/CONTROLLED_POSTGRESQL_FLYWAY_SMOKE_RUNNER.md"
  - "Add guarded controlled PostgreSQL Flyway smoke runner script"
  - "Add test-only controlled external PostgreSQL Flyway smoke that skips without env"
  - "Check controlled DB env presence without printing values"
  - "Run targeted smoke only in missing-env skip mode unless controlled non-production DB env is supplied"
  - "Update production-readiness docs and status-source docs for LIVE3"
forbidden_scope:
  - "production DB access"
  - "destructive DB operations"
  - "real secrets"
  - "committed .env"
  - "printing secret values"
  - "Java business logic"
  - "schema.sql"
  - "Flyway SQL migration changes"
  - "order execution"
  - "auto-open"
  - "auto-close"
  - "auto-reverse"
  - "auto-trading"
  - "external push send"
  - "fake positions"
  - "fake review records"
  - "production-ready claim"
checks:
  - "./mvnw -q -Dtest=ControlledPostgreSqlFlywaySmokeTest test"
  - "git diff --check"
  - "bash scripts/check-workflow-contract.sh"
  - "bash scripts/v1-delivery-check.sh"
  - "bash scripts/v1-state.sh"
  - "bash -n any new/changed scripts"
  - "YAML status files parse successfully"
stop_conditions:
  - "any single command would run longer than 5 minutes"
  - "change requires production DB access, real secrets, secret printing, or destructive DB operation"
  - "change would claim production readiness without explicit PASS evidence for every production gate"
  - "change requires order execution, auto-open, auto-close, auto-reverse, auto-trading, or external push send"
  - "change requires fake positions or fake review records"
  - "delivery check or v1-state reports a blocker unrelated to the current unmerged branch"
