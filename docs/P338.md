# P338 RuntimeKlineContextSourceBindingAssembler And Verification

## Package Type / 包类型

P338 is a B-risk Java/test assembler plus docs verification package in the Readiness / Point Mainline.

P338 是 Readiness / Point Mainline 中的 B 档 Java/test assembler + docs verification 包。

P338 adds only a plain Java `RuntimeKlineContextSourceBindingAssembler`, targeted assembler tests, and verification docs.

P338 只新增 plain Java `RuntimeKlineContextSourceBindingAssembler`、targeted assembler tests 和 verification 文档。

## Capability Movement / 能力移动

`RUNTIME_KLINE_CONTEXT_SOURCE_BINDING_VALIDATOR_JAVA_SKELETON -> RUNTIME_KLINE_CONTEXT_SOURCE_BINDING_ASSEMBLER_AND_VERIFICATION`

This movement closes the RuntimeKlineContext source binding skeleton chain at carrier + validator + assembler + verification level.

该能力移动把 RuntimeKlineContext source binding 骨架链路收口到 carrier + validator + assembler + verification 层级。

P338 does not raise Production Runtime Progress.

P338 不提高 Production Runtime Progress。

## Scope / 范围

P338 may add:

- `RuntimeKlineContextSourceBindingAssembler`;
- `RuntimeKlineContextSourceBindingAssemblerTest`;
- P338 verification docs;
- minimal status document updates.

P338 可以新增：

- `RuntimeKlineContextSourceBindingAssembler`；
- `RuntimeKlineContextSourceBindingAssemblerTest`；
- P338 verification 文档；
- 最小状态文档更新。

## Assembler Boundary / 组装器边界

`RuntimeKlineContextSourceBindingAssembler` only moves explicit `AssemblyInput` fields into `RuntimeKlineContextSourceBindingDTO`.

`RuntimeKlineContextSourceBindingAssembler` 只把显式 `AssemblyInput` 字段搬运到 `RuntimeKlineContextSourceBindingDTO`。

It immediately calls `RuntimeKlineContextSourceBindingValidator`.

它会立即调用 `RuntimeKlineContextSourceBindingValidator`。

It returns both:

- `RuntimeKlineContextSourceBindingDTO context`;
- `RuntimeKlineContextSourceBindingValidator.ValidationResult validationResult`.

它返回：

- `RuntimeKlineContextSourceBindingDTO context`；
- `RuntimeKlineContextSourceBindingValidator.ValidationResult validationResult`。

## Assemble Rules / 组装规则

Assembler behavior:

- null input -> `INCOMPLETE`;
- requested `BLOCKED_FAIL_CLOSED` -> DTO `blockedFailClosed(...)`;
- requested `REVIEW_ONLY_RUNTIME_KLINE_BINDING_DEGRADED` -> DTO `degraded(...)`;
- requested `REVIEW_ONLY_RUNTIME_KLINE_BINDING` -> DTO `reviewOnly(...)`;
- requested `INCOMPLETE` or unsupported status -> DTO `incomplete(...)`;
- every assembled context goes through validator.

组装行为：

- null input -> `INCOMPLETE`；
- requested `BLOCKED_FAIL_CLOSED` -> DTO `blockedFailClosed(...)`；
- requested `REVIEW_ONLY_RUNTIME_KLINE_BINDING_DEGRADED` -> DTO `degraded(...)`；
- requested `REVIEW_ONLY_RUNTIME_KLINE_BINDING` -> DTO `reviewOnly(...)`；
- requested `INCOMPLETE` 或不支持状态 -> DTO `incomplete(...)`；
- 每次组装后的 context 都必须经过 validator。

## Explicit Non-Scope / 明确非范围

P338 does not add:

- service;
- controller;
- mapper;
- repository;
- scheduler;
- resources;
- schema / config / pom;
- dashboard runtime;
- MarketQuoteClient;
- WebClient / RestTemplate / HttpClient / OkHttp / DataSource;
- RuntimeKlineContext real runtime reads;
- DataQualityContext;
- MultiTimeframeContext;
- RiskActionGuardContext;
- WatchlistPoolProof;
- source-owned candidate integration;
- internal preview;
- external channel;
- Push send;
- order;
- execution;
- auto-trading.

P338 不新增：

- service；
- controller；
- mapper；
- repository；
- scheduler；
- resources；
- schema / config / pom；
- dashboard runtime；
- MarketQuoteClient；
- WebClient / RestTemplate / HttpClient / OkHttp / DataSource；
- RuntimeKlineContext 真实运行读取；
- DataQualityContext；
- MultiTimeframeContext；
- RiskActionGuardContext；
- WatchlistPoolProof；
- source-owned candidate integration；
- internal preview；
- external channel；
- Push send；
- order；
- execution；
- auto-trading。

## Prohibited Behavior / 禁止行为

The assembler must not:

- read real market data;
- call external providers;
- calculate latestPrice / latestClose;
- calculate OHLCV;
- calculate wick / gap / liquidity / stampede;
- calculate entry / stop / TP / RR;
- derive entry from latestPrice;
- derive stop from latestClose;
- derive TP from OHLCV or RR formula;
- create sourceTraceRefs;
- use latestPrice-only instead of sourceTraceRefs;
- generate final direction;
- generate a trading action;
- send a message;
- call service or runtime provider paths.

assembler 不得：

- 读取真实行情；
- 调用 external providers；
- 计算 latestPrice / latestClose；
- 计算 OHLCV；
- 计算 wick / gap / liquidity / stampede；
- 计算 entry / stop / TP / RR；
- 从 latestPrice 推导 entry；
- 从 latestClose 推导 stop；
- 从 OHLCV 或 RR formula 推导 TP；
- 创建 sourceTraceRefs；
- 用 latestPrice-only 替代 sourceTraceRefs；
- 生成 final direction；
- 生成交易动作；
- 发送消息；
- 调用 service 或 runtime provider 路径。

## Verification / 验证收口

P338 verifies P335-P338:

- P335 defined the RuntimeKlineContext Source Binding Plan;
- P336 added the DTO carrier skeleton;
- P337 added the validator skeleton;
- P338 adds the assembler skeleton.

P338 验证 P335-P338：

- P335 定义 RuntimeKlineContext Source Binding Plan；
- P336 新增 DTO carrier skeleton；
- P337 新增 validator skeleton；
- P338 新增 assembler skeleton。

The current RuntimeKlineContext source binding chain has:

- carrier;
- validator;
- assembler;
- targeted tests;
- review-only boundary;
- incomplete-safe behavior;
- fail-closed blocked behavior.

当前 RuntimeKlineContext source binding 链路具备：

- carrier；
- validator；
- assembler；
- targeted tests；
- review-only 边界；
- incomplete-safe 行为；
- blocked fail-closed 行为。

## Still Not Complete / 仍未完成

P338 does not complete:

- real market reads;
- real RuntimeKlineContext runtime source;
- real SourceTrace runtime binding;
- DataQualityContext;
- MultiTimeframeContext;
- RiskActionGuardContext runtime integration;
- WatchlistPoolProof runtime integration;
- source-owned candidate integration;
- internal preview;
- dashboard;
- real entry / stop / TP / RR;
- external push;
- order / execution / auto-trading.

P338 仍未完成：

- 真实行情读取；
- 真实 RuntimeKlineContext runtime source；
- 真实 SourceTrace runtime binding；
- DataQualityContext；
- MultiTimeframeContext；
- RiskActionGuardContext runtime integration；
- WatchlistPoolProof runtime integration；
- source-owned candidate integration；
- internal preview；
- dashboard；
- 真实 entry / stop / TP / RR；
- external push；
- order / execution / auto-trading。

## Capability Conclusion / 真实能力结论

After P338, RuntimeKlineContext Source Binding reaches an L3-L4 skeleton closure:

- L0 Plan: complete;
- L1 DTO: complete;
- L2 Validator: complete;
- L3 Assembler: complete;
- L4 Verification: complete.

P338 后，RuntimeKlineContext Source Binding 达到 L3-L4 骨架闭环：

- L0 Plan：完成；
- L1 DTO：完成；
- L2 Validator：完成；
- L3 Assembler：完成；
- L4 Verification：完成。

It still has not reached:

- L5 internal wiring;
- L6 dashboard preview;
- L7 runtime-safe usable.

它仍未达到：

- L5 internal wiring；
- L6 dashboard preview；
- L7 runtime-safe usable。

Therefore P338 still cannot generate real points and cannot trade.

因此 P338 后仍不能生成真实点位，也不能交易。

## Next Safe Package / 下一安全包

Next safe package should enter `DataQualityContext Source Binding Plan` or a larger DataQuality DTO + Validator + Assembler capability closure package.

下一安全包应进入 `DataQualityContext Source Binding Plan`，或更大的 DataQuality DTO + Validator + Assembler 能力闭环包。

Do not split another RuntimeKlineContext verification micro-package.

不要再单独拆 RuntimeKlineContext verification 微包。

Do not jump directly to service runtime, dashboard runtime, real point generation, external push, order, execution, or auto-trading.

不要直接跳到 service runtime、dashboard runtime、真实点位生成、external push、order、execution 或 auto-trading。
