# P347 SourceOwnedCandidateIntegrationSourceBindingValidator Verification

## Package Type

P347 is an A-risk docs-only verification package in the Readiness / Point Mainline.

It verifies the P345 DTO and P346 validator stages for Source-Owned Candidate Integration Source Binding.

P347 does not add Java, tests, assembler, runtime wiring, service, dashboard runtime, external channel, Push send, order, execution, or auto-trading.

## Capability Movement

`SOURCE_OWNED_CANDIDATE_INTEGRATION_SOURCE_BINDING_VALIDATOR_JAVA_SKELETON -> SOURCE_OWNED_CANDIDATE_INTEGRATION_SOURCE_BINDING_VALIDATOR_VERIFICATION`

This movement is verification-only.

It does not make source-owned candidate integration runtime available.

It does not make candidate generation, point generation, final direction, push sending, or trading available.

## Verification Object

P347 verifies:

- P345 `SourceOwnedCandidateIntegrationSourceBindingDTO`;
- P345 `SourceOwnedCandidateIntegrationSourceBindingDTOTest`;
- P346 `SourceOwnedCandidateIntegrationSourceBindingValidator`;
- P346 `SourceOwnedCandidateIntegrationSourceBindingValidatorTest`.

P347 confirms:

- DTO is a plain Java carrier;
- validator is a plain Java safety gate;
- DTO and validator remain review-only;
- DTO and validator remain incomplete-safe;
- blocked conditions remain fail-closed;
- no runtime source binding has been connected;
- no source-owned candidate integration runtime has been created.

## Completed By P345

P345 added a DTO-only Java/test skeleton.

The DTO carries explicit future source-owned candidate integration source binding fields such as:

- SourceTrace refs;
- RuntimeKlineContext ref;
- DataQualityContext ref;
- MultiTimeframeContext ref;
- RiskActionGuardContext ref;
- WatchlistPoolProofContext ref;
- upstream source binding statuses;
- source binding completeness score;
- source summary safety flags;
- candidate boundary / unavailable / blocked / degraded labels;
- source-owned trace refs;
- missing / degraded / blocked reasons;
- forced safety flags.

P345 targeted tests verified:

- safety flags are forced true;
- `failClosed` is only true for blocked status;
- list fields are defensively copied and immutable;
- status enum values are present;
- no setter / builder / factory can disable safety flags;
- DTO source has no forbidden framework, provider, service, external channel, or trading dependencies;
- safe output does not contain executable semantics.

## Completed By P346

P346 added a validator-only Java/test skeleton.

The validator checks:

- null context -> `INCOMPLETE`;
- missing binding status -> `INCOMPLETE`;
- safety flags required true;
- required SourceTrace / RuntimeKlineContext / DataQualityContext / MultiTimeframeContext / RiskActionGuard / WatchlistPoolProof refs;
- source binding completeness score;
- upstream source present / trusted / review-only / not-trade-instruction / manual-review-required / incomplete-safe summaries;
- source blocked / incomplete / degraded summaries;
- untrusted source -> `BLOCKED_FAIL_CLOSED`;
- inherited RiskActionGuard and WatchlistPoolProof fail-closed boundaries;
- forbidden executable semantics.

P346 targeted tests verified:

- incomplete / blocked / degraded / review-only validation statuses;
- missing required refs;
- missing and low completeness score;
- required source summary false states;
- degraded reason requirement;
- forbidden executable semantics;
- untrusted source;
- validation result safety flags;
- no forbidden framework, provider, service, external channel, or trading dependencies.

## Current L0-L7 Status

- L0 Source Binding Plan: complete in P344.
- L1 DTO: complete in P345.
- L2 Validator: complete in P346.
- L3 Assembler: not complete.
- L4 Verification: P347 documents the DTO + validator stage closure.
- L5 Internal wiring: not complete.
- L6 Dashboard preview: not complete.
- L7 Runtime-safe usable: not complete.

P347 does not raise Production Runtime Progress.

## Current Source-Owned Candidate Integration Boundary

Current source-owned candidate integration is only:

- source binding plan;
- source binding DTO;
- source binding validator;
- verification documentation.

Current source-owned candidate integration is not:

- a runtime integration;
- an assembler;
- a candidate generator;
- a point generator;
- a push sender;
- a dashboard runtime;
- a service runtime;
- a trading execution layer.

The system has not actually wired SourceTrace, RuntimeKlineContext, DataQualityContext, MultiTimeframeContext, RiskActionGuard, and WatchlistPoolProof into one production source-owned candidate integration flow.

The system has not produced a source-owned candidate integration runtime object.

## Still Not Available

After P347, the system still cannot:

- generate real candidates;
- generate review-only point candidates;
- generate entry / stop / TP / RR;
- generate final direction;
- display runtime candidates on dashboard;
- send push messages;
- read real market data;
- read latest price;
- read latest close;
- read Watchlist service;
- connect service runtime;
- connect external channel;
- place orders;
- execute;
- auto-trade.

## Fail-Closed And Incomplete-Safe Conclusion

P347 confirms the expected source-owned candidate integration safety conclusions:

- any untrusted upstream source binding must become `BLOCKED_FAIL_CLOSED`;
- any upstream safety flag disabled must become `BLOCKED_FAIL_CLOSED`;
- RiskActionGuard blocked / stampede / wick-only reverse / liquidity-degraded one-shot-exit semantics must become `BLOCKED_FAIL_CLOSED`;
- WatchlistPoolProof disabled / empty / non-member / untrusted must become `BLOCKED_FAIL_CLOSED`;
- WatchlistPoolProof stale cannot pass silently and must become `INCOMPLETE` or fail-closed depending on context;
- Display Slots / default six home assets cannot be treated as Watchlist Pool Proof;
- executable semantics cannot enter source-owned candidate integration.

## Watchlist Pool Boundary

Watchlist Pool remains the maximum boundary for candidate / opportunity-push eligibility.

P347 reconfirms:

- Display Slots are not Watchlist Pool Proof;
- default home display is not Watchlist Pool Proof;
- default display does not authorize push;
- assets outside Watchlist Pool cannot enter candidate integration;
- P347 does not connect Watchlist service;
- P347 does not read rule config;
- P347 does not read audit tables.

## Risk Action Guard Boundary

RiskActionGuard remains an action review boundary, not an execution layer.

P347 reconfirms:

- high risk does not mean immediate stop;
- high risk does not mean immediate reverse;
- high risk does not mean immediate open;
- strong reversal does not mean direct reverse;
- wick-only does not mean trend reversal;
- stampede state blocks opportunity push, reverse, and new open semantics;
- liquidity degradation must not become one-shot market cut semantics;
- P347 does not generate executable risk actions.

## Verification Record

The P345 / P346 stage has recorded the required checks:

- `bash scripts/check-workflow-contract.sh`;
- `./mvnw -q -DskipTests compile`;
- `./mvnw -q -DskipTests test-compile`;
- `./mvnw -q -Dtest=SourceOwnedCandidateIntegrationSourceBindingDTOTest test`;
- `./mvnw -q -Dtest=SourceOwnedCandidateIntegrationSourceBindingValidatorTest test`;
- `git diff --check`;
- `git diff --cached --check`;
- `git diff --check main...HEAD`;
- forbidden path check.

P347 itself is docs-only and runs only docs-safe verification checks.

## Non-Scope

P347 does not add:

- Java;
- tests;
- assembler;
- service;
- controller;
- mapper;
- repository;
- scheduler;
- resources;
- schema;
- config;
- pom changes;
- dashboard runtime;
- source-owned candidate runtime;
- internal preview;
- external channel;
- Push send;
- order;
- execution;
- auto-trading.

P347 does not generate:

- real entry;
- real stop;
- real TP;
- RR;
- final direction;
- executable point values;
- executable trade actions;
- candidate runtime output.

## Next Safe Package

The next safe package is:

`P348 SourceOwnedCandidateIntegrationSourceBindingAssembler Plan`

Do not jump directly to:

- Java assembler implementation;
- source-owned candidate integration runtime;
- service runtime;
- dashboard runtime;
- executable point generation;
- external channel;
- Push send;
- order / execution / auto-trading.
