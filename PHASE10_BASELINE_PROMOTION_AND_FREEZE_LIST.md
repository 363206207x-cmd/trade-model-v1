# Phase 10 Baseline Promotion and Freeze List

## 文档目的

本文件用于在“不开新实现”的前提下，完成一次总控增量偏差复核后的统一归档：

1. 将本轮已落地增量纳入“可升版入基线”清单。
2. 明确区分“阶段收口（可冻结）”与“仅实现完成（暂不冻结终态）”。
3. 固化收口后维护顺序，避免继续局部补丁式推进。

结论时间：2026-04-26

---

## 一、可升版入基线（本轮确认）

以下增量已具备“纳入基线”条件，可作为后续审计与排期的统一口径：

- B 线：`derivativesCrowdingState` 二刀
- C 线：宏观第二刀 A1
- OI 第二刀：`openInterestDelta`
- 事件第一刀
- 宏观第一刀
- 评分已从 `5/8` 进入 `8/8`（新增 `事件冲击分`）
- execution 三刀

> 说明：此处“可升版入基线”代表“当前仓库已落地、可引用、可追溯”，不自动等同于对应模块达到规格终态。

---

## 二、可标“阶段收口”（可冻结）

以下项可明确标记为“阶段收口”，即当前口径可冻结并作为后续增量的边界锚点：

- `derivativesCrowdingState` 二刀 B
- `openInterestDelta` 最小口径
- 宏观证据 A1
- execution 对象真值写链
- 事件第一刀

> 冻结含义：冻结的是“当前最小口径与边界”，不是“完整模块终态完成”。

---

## 三、仅实现完成，暂不冻结终态

以下项目前仅可判定为“实现完成”，暂不应宣告终态冻结：

- 评分整体仍属最小实现（当前已进入 `8/8`，仍非八大评分完成态）
- 宏观线整体
- 事件线整体

> 管理要求：对外叙事与内部评审均应显式标注“仍属最小实现”，禁止将其误读为完整模块交付。

---

## 四、收口后维护顺序（拍板）

### 4.1 评分线状态

评分线当前状态固定为：

- 已进入 `8/8`，并阶段收口为最小评分闭环
- 当前不继续扩评分主线，转入维护/回归状态

### 4.2 事件线顺序

- 事件第二刀后置，待独立评审后再开。

### 4.3 决策依据（固定口径）

本次维护顺序遵循“小步、单刀、低扩面”策略，核心理由如下：

1. 评分线已完成 `8/8` 最小收口，不需要再以“补项数”为目标开新评分主线。
2. `scoreTopItems(top3)` = 同源 brief 裁剪视图（按记录顺序），不代表按 scoreValue 排序，不代表评分优先级排序，不等于完整评分解释模块。
3. 当前应优先做轻量回归与口径维护，避免评分线扩面引入跨模块扰动。

---

## 五、执行约束（本轮）

本轮仅做归档，不开新实现。后续执行顺序固定为：

1. 以本文件作为“基线升版清单（冻结 / 未冻结双清单）”权威入口。
2. 以本文件为基线，执行评分线维护/回归，不新增评分实现。
3. 事件第二刀维持后置，待前置条件成熟后单独评审。

---

## 一句话决议

先完成基线升版归档，评分线转入维护/回归；事件第二刀后置。
