# Phase 9 Step 3 - Acceptance Record Template

## 文档用途

本模板用于记录「第九阶段 Step 3：Review 聚合分层（摘要 vs 明细）与性能护栏」的实际验收执行结果，可作为：

- 开发自查留档
- 测试联调留档
- 阶段验收附件

建议与以下文档配套使用：

- `PHASE9_STEP3_REVIEW_AGGREGATE_LAYERING_CONTRACT.md`

## 基本信息

- 阶段：Phase 9
- Step：Step 3
- 验收主题：Review Aggregate 分层与性能护栏
- 验收环境：`________________________`
- 接口基地址：`________________________`
- 验收批次/轮次：`________________________`
- 前端分支/版本：`________________________`
- 后端分支/版本：`________________________`

## 1) 验收执行记录表（主表）

> 固定列：验收项 / 预期结果 / 实际结果 / 证据链接截图接口返回 / 是否通过 / 负责人 / 日期 / 备注

| 验收项 | 预期结果 | 实际结果 | 证据链接/截图/接口返回 | 是否通过 | 负责人 | 日期 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 兼容接口可用（`GET /api/review/aggregate/{analysisId}`） | 返回 `200`，历史调用不受影响 | `________________` | `________________` | `通过 / 不通过` | `________________` | `YYYY-MM-DD` | `________________` |
| 摘要接口可用（`GET /api/review/aggregate/{analysisId}/summary`） | 返回 `200`，可用于首屏轻载 | `________________` | `________________` | `通过 / 不通过` | `________________` | `YYYY-MM-DD` | `________________` |
| 明细接口可用（`GET /api/review/aggregate/{analysisId}/detail`） | 返回 `200`，支持按 section 懒加载 | `________________` | `________________` | `通过 / 不通过` | `________________` | `YYYY-MM-DD` | `________________` |
| summary 核心字段完整性 | 至少包含 `run/decision/plan/reviewClosure/detailSections` | `________________` | `________________` | `通过 / 不通过` | `________________` | `YYYY-MM-DD` | `________________` |
| detail 默认 section 行为 | section 为空时默认落到 `pushRecheck` | `________________` | `________________` | `通过 / 不通过` | `________________` | `YYYY-MM-DD` | `________________` |
| section=`pushRecheck` 分发正确 | 返回 `pushRecheck` 明细区 | `________________` | `________________` | `通过 / 不通过` | `________________` | `YYYY-MM-DD` | `________________` |
| section=`missed` 分发正确 | 返回 `missed` 明细区 | `________________` | `________________` | `通过 / 不通过` | `________________` | `YYYY-MM-DD` | `________________` |
| section=`alerts` 分发正确 | 返回 `alerts` 明细区 | `________________` | `________________` | `通过 / 不通过` | `________________` | `YYYY-MM-DD` | `________________` |
| section=`ruleVersionLogs` 分发正确 | 返回 `ruleVersionLogs` 明细区 | `________________` | `________________` | `通过 / 不通过` | `________________` | `YYYY-MM-DD` | `________________` |
| section=`hotReset` 分发正确 | 返回 `hotReset` 明细区 | `________________` | `________________` | `通过 / 不通过` | `________________` | `YYYY-MM-DD` | `________________` |
| 非法 section 错误语义 | 非法 section 返回 `400` | `________________` | `________________` | `通过 / 不通过` | `________________` | `YYYY-MM-DD` | `________________` |
| analysis 不存在错误语义（summary） | 不存在 analysisId 返回 `404` | `________________` | `________________` | `通过 / 不通过` | `________________` | `YYYY-MM-DD` | `________________` |
| analysis 不存在错误语义（detail） | 不存在 analysisId 返回 `404` | `________________` | `________________` | `通过 / 不通过` | `________________` | `YYYY-MM-DD` | `________________` |
| limit 默认值保护 | `limit<=0` 时 `limitApplied=20` | `________________` | `________________` | `通过 / 不通过` | `________________` | `YYYY-MM-DD` | `________________` |
| limit 上限保护 | `limit>50` 时 `limitApplied=50` | `________________` | `________________` | `通过 / 不通过` | `________________` | `YYYY-MM-DD` | `________________` |
| 明细边界字段完整性 | 返回 `limitApplied/total/truncated` | `________________` | `________________` | `通过 / 不通过` | `________________` | `YYYY-MM-DD` | `________________` |
| pushRecheck 内部二次限流 | 单条 push 的 `rechecks` 条数不超过 `5` | `________________` | `________________` | `通过 / 不通过` | `________________` | `YYYY-MM-DD` | `________________` |
| 前端接入顺序符合 contract | 先 summary 后按需 detail，无首屏全量明细并发 | `________________` | `________________` | `通过 / 不通过` | `________________` | `YYYY-MM-DD` | `________________` |
| 自动化回归（服务层） | 相关测试执行通过 | `________________` | `________________` | `通过 / 不通过` | `________________` | `YYYY-MM-DD` | `________________` |

## 2) 接口调用证据区（建议）

### 2.1 摘要接口证据

- 请求 URL：
  - `________________`
- 请求参数：
  - `________________`
- 响应摘要（code/msg/核心字段）：
  - `________________`
- 原始返回保存位置（文件、截图、日志）：
  - `________________`

### 2.2 明细接口证据（建议至少覆盖 3 个 section）

- 请求 URL：
  - `________________`
- 请求参数（section/limit）：
  - `________________`
- 响应摘要（limitApplied/total/truncated/条数）：
  - `________________`
- 原始返回保存位置（文件、截图、日志）：
  - `________________`

### 2.3 异常语义证据（400/404）

- 用例描述：
  - `________________`
- 请求 URL：
  - `________________`
- 响应摘要（HTTP 状态码 + msg）：
  - `________________`
- 证据位置：
  - `________________`

## 3) 自动化测试证据区（建议）

- 执行命令：
  - `mvn test -Dtest=ReviewAggregateServiceImplTest`
- 结果摘要：
  - `________________`
- 报告路径或截图：
  - `________________`
- 补充回归命令（如有）：
  - `________________`

## 4) 最终验收结论区

- 本轮结论：`通过 / 有条件通过 / 不通过`
- 阻塞项（如有）：
  - `________________`
- 风险项（如有）：
  - `________________`
- 后续动作：
  - `________________`
- 结论确认人：
  - `________________`
- 结论日期：
  - `YYYY-MM-DD`

---

## 附：可直接复制的空白行模板

| 验收项 | 预期结果 | 实际结果 | 证据链接/截图/接口返回 | 是否通过 | 负责人 | 日期 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `________________` | `________________` | `________________` | `________________` | `通过 / 不通过` | `________________` | `YYYY-MM-DD` | `________________` |
