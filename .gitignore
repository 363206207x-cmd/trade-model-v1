# Maven/Java build output
target/

# Local Maven repositories/cache
.m2-local/
.m2/

# IDE/editor metadata
.idea/
.vscode/
*.iml

# OS metadata
.DS_Store

# Logs and temp files
*.log
*.tmp
*.swp

# H2 file database artifacts (local dev only)
# NOTE: `data/` here only refers to local H2 file DB / local dev data.
# It must not be treated as business data for repository submission.
data/
*.mv.db
*.trace.db
*.lock.db

# Local temporary backup artifacts
backups/
docs/residual-backups/
